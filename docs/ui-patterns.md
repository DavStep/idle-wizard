# UI Patterns and Reusable Widget Library

This is the authoritative reusable widget library for Idle Wizard. Before adding UI, map every visible or interactive building block to an entry here and reuse its contract.

## Admission Rule

- Classify each building block as `reuse`, `extension`, or `new widget` before editing product UI code.
- `reuse` uses a library entry unchanged. `extension` supplies feature data, copy, or composition without changing the entry's visual, interaction, state, or accessibility contract.
- `new widget` includes a new primitive, compound component, scroll behavior, box/dialog type, control pattern, or a meaningfully different variant of an existing entry.
- Before adding any new widget, ask the user to approve one batched list with exact names, purpose, closest existing entry and why it is insufficient, supported states, intended consumers, and labeled `390x844` visual previews.
- Keep proposal previews disposable and outside product runtime code. After approval and implementation, add the widget to this library with its stable source, contract, and real-app preview evidence.
- A proposal preview must be a visible image or rendered artifact, not only prose or an ASCII wireframe. Include a native-pixel crop for component judgment; a labeled contact sheet may preview the whole batch.
- Wait for explicit approval of each named widget. A screen-level approval does not cover unlisted widget dependencies.
- Every completed UI task reports `New widgets: none` or lists the approved widgets introduced.

## Current Library

| Widget | Stable source | Contract and approved use |
| --- | --- | --- |
| Ordinary border-labeled box | `.style-box`, `.style-box__title` in `src/styles/base.css` | Default room section with compact padding, embedded transparent title, no dialog shadow. |
| Expandable inventory box | `src/pages/shared/RoomInventoryBoxManager.js` | Ordinary box with count label, collapsed summary rows, and bottom-border expand/collapse action. |
| Default player dialog shell | `PixiDialogFrame` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; `.style-dialog`, `.style-box__title` in `src/styles/base.css` for fallback markup | Root Run Expedition brown outer frame with a fixed `304px` base content width and `53px` minimum content height, paper content, purple title plaque, detached round close control centered below the shell when dismissal is allowed, and modal shadow. Named wider or fixed-height variants may override the base geometry. All game dialogs, including blockers and the first tutorial story prompt, use this shell. |
| Split-paper dialog composition | `createDialogPaperSection`, `resolveDialogPaperOutsets`, and `setDialogPaperSectionBounds` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; first composition in `ShopDialogPixi` | Reuses the exact Expedition paper nine-slice for two or more full-width sections inside one brown dialog shell. Hide the shell's continuous paper, keep content on the standard dialog column with no feature-local side inset, expand each paper frame through the shared `20px` dialog padding, and preserve an `8px` visible paper-to-paper gap. Section content begins `5px` below its content boundary; use the shared row pitch and feature-owned row gap. First consumer: Load Stall; Level rewards reuses the same composition. |
| Level Rewards Dialog | `PixiLevelDialog` in `src/rendering/pixi/global/dialogs/PixiLevelDialog.js`; `.room-top-panel__level-dialog--board-backed` in `src/styles/base.css` | Feature composition of the default player dialog shell and the existing split-paper pattern. The gained and total groups each use a full-width Expedition paper section, while fixed yellow previous/next buttons sit inside the lower board corners. The pager supports previous-hidden, both-visible, and next-hidden states without shifting its side anchors. All labels use Title Case and resource amounts keep their icons. |
| Danger Dialog Title Plaque | `PixiDialogFrame` with `titleVariant: 'danger'` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; `.style-dialog--danger` in `src/styles/base.css` for fallback markup | Approved destructive or loss-bearing title variant. It preserves the default Expedition plaque geometry, ornaments, white outlined title text, dialog shell, close behavior, and motion while hue-shifting only the purple plaque art to semantic red. It has no selected, disabled, loading, error, empty, overflow, or separate reduced-motion state. First consumer: Garden `Cancel Progress?`. Evidence: `screenshots/garden-cancel-danger-390x844.png`, `screenshots/garden-cancel-danger-native-crop.png`, `screenshots/garden-cancel-danger-desktop-fitted.png`, and `screenshots/garden-cancel-danger-reference-review.html`. |
| Full-screen announcement screen | `PixiAnnouncementSurface` in `src/rendering/pixi/global/dialogs/PixiMessageDialogs.js`; `.room-announcement-layer` in `src/styles/base.css` | Centered, unframed progress, research, level-up reward, and feature-unlock presentation over the full-screen backdrop. Level-up rewards reuse the Market ribbon asset for the Title Case `Level Up!` heading, stage dark retained rows with left white outlined labels and right icon-backed values, then remain open on `Tap to continue`; reduced motion settles the complete composition immediately. Preview screens dismiss from the backdrop and do not show a dialog close control; report-style announcements opt into the default dialog shell. |
| Base text button | `.style-button` in `src/styles/base.css` | Shared compact button foundation; feature classes may size or place it without inventing new state treatment. |
| Regular button color variants | `.style-button--yellow`, `.style-button--green`, `.style-button--red`, `.style-button--brown-dark`, `.style-button--brown-light` in `src/styles/base.css`; `PixiButton` in `src/rendering/pixi/primitives/PixiButton.js` | Shared straight-edged Root Run regular-button geometry. Choose color by role: green for positive apply/mark actions, red for enabled cancel/clear actions, yellow for highlighted actions, and brown for neutral/tab roles. Disabled variants swap to the shared gray asset without a grayscale or monochrome shader. The Account dialog is the approved source-authored exception: `account-tab` resolves to its exact active/inactive skins and `account-save` uses its exact larger green silhouette, while retaining the same button input, semantic, press, and disabled contracts. |
| Cost button | `src/pages/shared/CostButtonManager.js`, `.style-cost-button`; `PixiCostButton` in `src/rendering/pixi/primitives/PixiCostButton.js` | Price/cost control with icon-before-value presentation, unaffordable and disabled states, accessible label, and isolated click handling. |
| Stacked Cost Button | `PixiCostButton` with `stacked: true` in `src/rendering/pixi/primitives/PixiCostButton.js` | Approved retained purchase variant for a compact explicit action above an icon-and-amount row. It uses the narrower Root Run green/gray whole-button assets at `92x52`, requires both `actionLabel` and `amountLabel`, and preserves the base cost button’s available, unaffordable, disabled, press, focus, semantic-target, and callback contracts. No selected, loading, error, empty, or decorative-motion variants. First consumer: the Brewing locked-cauldron `Unlock` action. |
| Base input | `.style-input` in `src/styles/base.css` | Shared text, number, textarea, and select foundation with normal, focus, placeholder, and disabled states. |
| PixiTextField: brown-inset | Default `PixiTextField` skin in `src/rendering/pixi/primitives/PixiTextField.js`; explicit `variant: 'brown-inset'` remains supported | Approved default retained text-entry treatment for message composers, usernames, profile or guild naming, feedback, numeric entry, and other player-authored inputs. It uses `assets/game/source/ui/root-run-world-chat/world-chat-text-field-9slice.png` with symmetric `26px` source slices, cream value/caret text, muted warm placeholder copy, selection tint, and a restrained gold focus inset. Consumers vary only dimensions, input kind, placeholder, and behavior; do not introduce feature-local input chrome. Use explicit `variant: 'control'` only when an approved specialized surface requires the legacy frame. The Root Run Account composition alone uses `account-username` to place the same native text-entry behavior transparently over its exact username-row raster with authored white outlined type. It preserves native text-entry, max-length, submit, cancel, change, keyboard-inset, focus, multiline, and disabled-by-caller contracts. First consumer: the World Chat composer. Visual evidence: `screenshots/world-chat-text-field-390x844.png`, `screenshots/world-chat-text-field-native-crop.png`, `screenshots/world-chat-text-field-desktop-fitted.png`, and `screenshots/world-chat-text-field-reference-review.html`. |
| Managed vertical scroll pane | `.style-page-scroll`, `src/pages/managers/ScrollCueManager.js` | The only scroll behavior. Use it for full room pages, dialog/popup content, and bounded lists; context changes sizing, not physics or scrollbar type. |
| Progress rail | `.style-progress`, `.style-progress__fill`, `src/pages/shared/progressFill.js` | Shared timer/progress geometry and width-driven fill with reduced-motion handling; use approved room color roles only. |
| Amount selection row | `src/pages/shared/AmountSelectionRow.js` | Shared decrement/value/increment control with editable numeric value and accessible step labels. |
| Numbered or inline-action row | `Numbered Rows` and `Inline Row Actions` below | Compact left marker, middle content, fixed right action/status rhythm. |
| Research station-upgrade row | `ResearchRowWidget` in `src/rendering/pixi/pages/research/ResearchPixiPage.js`; `.research-page__row` in `src/styles/base.css` | Compact `80px` research catalogue row using the shared Root Run station-upgrade card, fitted between the shared content edges so the scroll viewport and right-edge scrollbar cannot clip it, with a scaled left art well, an adaptive middle description, a compact rank badge, and a scaled right cost/status control. The description uses the full fixed middle region and keeps the normal `11px` source size when possible, shrinking toward `8px` only when its wrapped height would overflow. Its measured vertical center includes a `-10px` optical correction so Lilita One's visible glyphs, rather than its low-sitting line box, center against the art and action. Locked rows reuse the normal card, art well, artwork, and ink under a `30%` black overlay; completed rows show `Researched` in a yellow recolor of the exact green cost-button silhouette. The title and description share the row ink color. |
| Research Station Title Plaque | `ResearchStationTitlePlaque` in `src/rendering/pixi/pages/research/ResearchPixiPage.js`; `.research-page__box-title` in `src/styles/base.css` | Static Research category heading using the Root Run `header-red` horizontal nine-slice geometry with tab-owned color variants: Regular is yellow, Automation is red, Advanced is emerald green, and Crystal is purple. It connects directly to the screen's left edge at `42px` source height while Research cards keep their normal `16px` inset. It uses a single-line Title Case `18px` Lilita One label and measures its width from the rendered label plus the tightened decorated right-cap allowance. Category rhythm is `18px` above the plaque and `5px` below it. Width caps at the right content edge; only an overlong label may reduce toward the normal `13px` source size. The visible title still names the category, so tab color is not the only identifier. No selected, disabled, loading, error, empty, or motion variants. Approved Pixi evidence: `screenshots/research-title-variants-native-contact-sheet.png`, the four `screenshots/research-title-*-390x844.png` authored views, and `screenshots/research-title-variants-desktop-fitted.png`. |
| Market Title Ribbon | `MarketTitleRibbon` in `src/rendering/pixi/pages/shop/MarketTitleRibbon.js` | Static Market licence heading using the approved purple folded-ribbon PNG as a horizontal nine-slice with fixed authored height and `73 27 73 27` source margins. The ribbon alone may stretch while its tails, folds, and outline remain fixed. The Title Case licence name and all three rank-star slots form one measured group centered horizontally on the ribbon front and shifted `6px` upward at the authored `390px` surface to align with the raised panel rather than the lower folds. The ribbon has no separate jewel or left-side decoration. Overlong names shrink toward the normal `13px` source floor while the star geometry stays fixed. Market is the only consumer for now. No selected, disabled, loading, error, empty, or motion variants. Visual evidence: `screenshots/market-title-ribbon-fixed-390x844.jpg`, `screenshots/market-title-ribbon-fixed-native-crop.jpg`, and `screenshots/market-title-ribbon-fixed-desktop-fitted.jpg`. Stretch evidence: `screenshots/market-title-ribbon-9slice-qa.png`. |
| Room inventory opener | `src/pages/shared/RoomInventoryButtonManager.js` | Shared icon-over-label side control for opening the seed, herb, or potion inventory surface. |
| Workshop HUD side opener | `WorkshopIconPanelAction`, `WorkshopFeatureButton`, and `layoutSideControls` in `src/rendering/pixi/pages/workshop/WorkshopPixiPage.js` | Existing Workshop icon-over-label controls participate in one left/right stack. Each model supplies `side` plus numeric `weight`; visible controls sort by weight and pack from the shared top anchor into fixed slots without hidden gaps. The approved family is the red hanging Alliance banner, trophy, task scroll, Bag, purple hat, green envelope, purple discovery journal, and checked event scroll. Per-icon scales normalize visible silhouette weight at a slightly reduced size rather than matching canvas dimensions. Alliance applies the same scale to its base and tintable cloth. Enter/exit and slot shifts use bounded transform/alpha motion, and reduced motion snaps. |
| Bottom room tab | `src/pages/bottomPanel/managers/BottomPanelViewManager.js`; `src/rendering/pixi/global/chrome/PixiBottomPanelView.js` | Fixed-width midnight top-cap tabs fill the source width and stretch their lower nine-slice region to the screen edge. The dark and lighter selected assets share `83 91 73 1` nine-slice geometry; active/inactive caps are `56px`/`44px` before the shared `26px` lower stretch. The selected tab is always `6px` wider, while each visible-tab count derives one fixed inactive width so the row remains edge-to-edge. Unlocked icons remain full-color and full-opacity, with non-selected icons rendered at `1.5x` their base artwork size. At seven visible tabs, every unlocked icon switches to `1.05x` inactive and `1x` selected compact scale. Selection applies the existing selected-icon treatment and lifts the icon `12px` over the retained `205ms` transition, while the active tab alone shows the white Title Case room name. Reduced motion snaps to the selected geometry. Locked tabs replace the room icon with a larger lock centered lower in the inactive frame. |
| Compact world chat | `src/pages/workshop/managers/WorkshopWorldChatManager.js`; `src/rendering/pixi/global/chrome/PixiWorldChatView.js` | Shared room chrome directly above the bottom tabs on every page after its existing level gate. Shows only the latest two one-line messages, bottom-aligned by default when fewer than two rows are present. The complete title, preview, padding, and frame form one opener that uses the shared confirmed press/release animation, light touch haptic, cancellation, and reduced-motion behavior before opening the existing full chat dialog without changing rooms. |
| WorldChatMessageRowPixi | `WorldChatMessageRowPixi` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved compact retained row for the full World Chat dialog. Player rows use a `22px` avatar, a two-line header/body rhythm, colored bracketed clan tag with the only text stroke in the row, dark-brown username/body copy, and a muted parchment-gold age at the far right. Chat rows use the paper width with an `8px` side inset instead of the generic dialog content column. Sparse message sets align their final row to the bottom of the viewport by default; overflowing histories retain the compact top inset and open pinned to the newest row. The avatar and username alone open Player Info; the row never shows action-button chrome and never shows player level. System rows are passive and replace the avatar with a warm borderless rounded background. Rows support player/system data, optional clan tag and color, wrapped body copy, timestamp, enabled Player Info action, and pooled reset/destroy states. Visual evidence: `screenshots/world-chat-approved-390x844.png`, `screenshots/world-chat-approved-native-crop.png`, `screenshots/world-chat-approved-desktop-fitted.png`, and `screenshots/world-chat-approved-reference-review.html`. |
| Popup text tab | `src/pages/shared/selectedTabState.js`, popup tab rules in `src/styles/base.css` | Brown text-tab family below dialogs with explicit selected semantics and fixed content above. |
| Resource or item icon label | `src/pages/shared/resourceIconLabel.js`, `src/pages/shared/itemIconLabel.js` | Shared icon-before-text label that preserves surrounding normal/disabled text color and accessible text. |
| Seed pack icon | `src/assets/items/seeds/seedIconFrames.js`, `src/assets/items/seeds/seedIcons.js`, `src/rendering/pixi/primitives/PixiSeedPackIcon.js` | One shared composite icon across DOM and retained Pixi: the transparent seed-pack base plus the matching herb mark at `44%` scale, centered at `63%` of the pack height on the sack body, and rotated `6deg`. Callers may size the icon but must not replace its frame resolution, aspect-fit, or internal herb placement with feature-local geometry. |
| Info icon button | `src/pages/shared/infoButton.js` | Shared decorative info asset; caller owns accessible name, geometry, and disclosure behavior. |
| Lock/check status icon | `src/pages/shared/statusIcon.js` | Shared status artwork; accompanying control or text owns the accessible meaning. |
| Notification badge | `src/pages/shared/notificationBadge.js`; `PixiNotificationBadge` in `src/rendering/pixi/global/transient/PixiNotificationBadges.js` | Red/orange priority indicator with tutorial visibility policy; not a general decoration dot. Every DOM and retained-Pixi path uses the shared `notification-circle-red.png` / `notification-circle-orange.png` sprite at `12px`, matching Root Run's `36px` station badge at the project `/3` source scale. Regular buttons keep the badge fully inside and tangent to the top-right edges; tabs inset it `4px` from both edges, matching Root Run's `12px` tab padding. Keep bottom-tab badges in the shared overlay above neighboring widgets. |
| Root Run Player HUD | `PixiTopPanelView` in `src/rendering/pixi/global/chrome/PixiTopPanelView.js` | Approved shared top-chrome compound at Root Run's exact qUIck `/3` geometry. It composes avatar/username at left, level rail above three equal currency capsules, mana generation rate below mana, and settings at right. The middle capsule is page-contextual and defaults to crystal. FTUE may hide the full top group, avatar, level rail, or resource controls without reflow. Evidence: `screenshots/root-run-top-hud-390x844.png` and `screenshots/root-run-top-hud-native-crop.png`. |
| HUD Avatar Button | `RootRunHudAvatarButton` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `186x186px` qUIck-source avatar control using the Root Run nine-slice frame and head background. It supports character portrait replacement, equipped-frame tint, enabled press, FTUE visibility, and direct Account-dialog activation. The player username is composition-owned and centered immediately below this widget. |
| HUD Level Rail | `RootRunHudLevelRail` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `662x93px` qUIck-source level star and progress rail. It uses Root Run's fixed panel/rounded track shape, a fixed Idle Wizard purple-to-gold gradient fill, one thin separator per request boundary, and no visible progress fraction or remaining-count copy. It supports hidden-at-level-zero, partial fill, completed separators, rollover, and reduced-motion completion. |
| HUD Currency Capsule | `RootRunHudCurrencyCapsule` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `208x66px` qUIck-source image-backed resource capsule. It supports coin, crystal, ruby, emerald, and mana icons; compact formatted values; contextual resource replacement; and FTUE visibility. The compound HUD owns the optional rate label below mana. |
| HUD Square Icon Button | `RootRunHudSquareIconButton` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `122x122px` qUIck-source image-backed icon action. Its first consumer is the top-panel settings gear, with retained press routing, semantic activation, haptic feedback, and FTUE visibility. New icon meanings reuse this widget only when the same square silhouette and interaction contract are appropriate. |
| Tooltip | `.style-tooltip` in `src/styles/base.css` | Small active-theme explanatory surface with compact offset shadow; not a replacement for persistent labels. |
| Reward / spend flyout | `src/pages/shared/RewardFlyoutManager.js`; `PixiTransientEffectsLayer` in `src/rendering/pixi/global/transient/PixiTransientEffectsLayer.js` | Shared pooled, bounded transient feedback with reduced-motion support. Successful foreground consumption uses the consumed resource icon and the Root Run seven-icon ballistic burst, anchored to the action that spent it; failed actions emit nothing. |
| Cauldron Carousel | `BrewingHudPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Brewing station selector with one centered cauldron, previous/next arrows, horizontal swipe, an inside-top-left cauldron title with the shared three-slot star rank, and dynamic dots for unlocked cauldrons plus one next locked slot. At the five-cauldron cap no extra locked dot is rendered. |
| Illustrated Action Button | `RetainedButton` compositions in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js`; green/gray Root Run skins in `src/rendering/pixi/primitives/PixiButton.js` | Image-backed compact action with neutral brown, positive green, highlighted yellow, danger brown-dark, and disabled gray states. Disabled state swaps to gray artwork with no shader. Brewing may lead the visible label with a retained illustrated icon while preserving the same bounds, semantic target, press motion, and accessible label; recipes, autobrew, brew quantity, settings, and cancel use this icon-plus-label composition. |
| Brewing Batch Detail Panel | `BrewingHudPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Brewing-only compound panel for potion art/name, owned and batch quantities, requirements, phase/timer progress, fast-forward placeholder, cancellation, and whole-batch collection. |
| Ingredient Requirement Tile | `BrewingIngredientRequirementTile` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Item-art requirement cell with owned/required quantity and sufficient, insufficient, empty, and decorative-extra states. Brewing always shows six cells in 3 columns by 2 rows while gameplay remains capped at five ingredients. |
| Automation Toggle Row | `BrewingAutomationSettingsDialogPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Per-cauldron persisted auto-collect toggle inside the shared dialog shell. It is enabled only while autobrew is enabled and defaults off. |
| Transient Toast | page-local toast in `BrewingPixiPage` | Short non-modal feedback anchored over the Brewing HUD; visible for 1.6 seconds and never blocks input. Currently used by timer fast-forward to report `coming soon`. |

Brewing widget approval and real-app evidence: `screenshots/brewing-redesign-390x844.png`,
`screenshots/brewing-redesign-native-crop.png`, and
`screenshots/brewing-redesign-settings-390x844.png`.

The managed vertical scroll pane intentionally has one behavior with three sizing contexts. Do not create separate room, dialog, or list scroll physics unless the user approves a new widget.

## Previewing Existing Widgets

- Start the dev UI editor as documented in `src/dev/uiEditor/README.md`, select an existing runtime widget, and use `open prefab` for an isolated preview.
- Prefer an existing `?devUi=<surfaceId>` route or checked-in UI recipe for full compositions. Add a new checked-in preview route only after its widget is approved.
- Proposal-only previews remain disposable. Approved implementation QA uses the real app at `390x844`, plus a native-pixel crop for the library entry.

## Box Construction

- The existing ordinary border-labeled box keeps compact padding, no added shadow, and its current themed frame contract. New default room landmarks and illustrated panels require separately approved image-backed widgets; do not mutate this legacy box into an all-purpose fantasy card.
- In selectable themes, standard `style-box` inner sections and `.style-panel.room-top-panel` use `inner-section-panel-<theme>-9slice.png` with CSS slice order `91 73 90 83 fill`. Its rendered border widths are those same qUIck margins scaled from the `1080px` export width to the `390px` logical surface; never substitute arbitrary compact border widths. Keep the exact center-sample color behind the border image so fractional contain scaling cannot expose transparent hairlines at slice joins. Do not reuse that skin for other outer `style-panel` surfaces, dialogs, buttons, tabs, tutorial surfaces, or feature-owned paper art.
- Box titles sit embedded transparently over the top border, without a background backplate.
- Box titles use the shared surface-colored text stroke in every selectable theme, keeping the border visible around the glyphs without creating a rectangular title surface.
- Secondary border labels, such as counts, current controls, actions, and tabs, use smaller normal-weight text centered on the border line.
- Default player-facing popup/dialog boxes use the shared Root Run Expedition dialog shell: fixed `304px` content width and `53px` minimum content height produce the standard `364px` brown outer nine-slice; `352px` Expedition paper nine-slice inset `6px` horizontally, `31px` from the shell top, and `21px` from the compact shell bottom; separate `222x44px` purple title plaque; `41px` round X close asset centered `23px` below the shell; and one compact bottom-right shadow. Named wider or fixed-height variants may override the base geometry. Plaque text uses the exported `64px`/`8px` source type/stroke metrics (`23.1px`/`2.9px` at `390px`) and the source `22px` top offset. These image layers stay transparent behind their PNG corners; never add a full rectangular fallback fill beneath them.
- `RetainedGlobalDialog` consumers use the same shell with a stricter responsive cap: the complete brown frame is at most `324px`, leaving `5%` on both sides of the `360px` retained source screen. That yields a `304px` core and `264px` maximum content box after shared padding. Reflow rows, grids, and external tab spans to that width; do not scale the retained display tree.
- App-level boot, connection, account, and deploy blockers use the shared player-dialog shell. Keep them non-dismissible when the flow must retain control; hiding the close action is a state of the existing shell, not a separate blocker panel.
- Every managed scroll panel uses `style-page-scroll` on the actual scrolling element. Full-page room roots additionally apply the shared `--style-page-scroll-padding-top` / `--style-page-scroll-padding-bottom` cuts so content tucks under chrome consistently. The Workshop Bag is the retained-dialog geometry reference: keep rows at full content width, use the shared `12px` source top inset, and extend the viewport `4px` toward the paper edge for the scrollbar instead of narrowing rows. Component-owned larger title clearance still wins. Dialogs, popups, and bounded lists use the same Root Run station scroll behavior and vertical scrollbar without introducing a second scroll type.

## Numbered Rows

- Numbered game rows use a narrow left number cell, written as `1.`, `2.`, etc.
- Main item/content text sits in the middle.
- Price, status, timer, or action sits right-aligned in one fixed right slot.
- Never leave the middle content blank. Unlocked empty slot rows should invite the next action, such as `select` or `request item`; locked empty rows still name the thing, such as `empty stand` or `empty request`, with the right slot carrying requirement/state.
- Item market rows without numbers use the same compact middle/right rhythm as numbered rows; do not give stock/picker rows looser vertical padding.
- Do not write row type words such as `stand 1` when the surrounding box already gives the type.
- Market stand rows, player request rows, and garden plot rows should follow this same left-number, middle-content, right-action rhythm.

## Inline Row Actions

- Row actions are text controls on the line, not separate decorative cards.
- Purchase prices use the shared DOM `CostButtonManager` or retained `PixiCostButton`: the caller supplies the formatted cost, current availability, accessible action label, and purchase callback. The control owns the approved Root Run green/gray artwork, Lilita One outlined amount, icon-before-value geometry, disabled state, and click isolation; gameplay facades still own affordability checks and spending.
- Brewing `recipes`/primary/quantity/auto controls use the yellow Root Run button configuration through `style-button--yellow`. Workshop `Summon Seed` reuses the approved `92x52` stacked `PixiCostButton` content layout with an `11px` action label above a mana-icon-and-amount row, but uses the shorter cost-button background configuration. Its `blue` tone preserves the short green silhouette, shared black text strokes, disabled gray skin, press, input, and notification contracts while recoloring only the enabled skin to the shared info icon's blue/cyan palette. The Brewing locked-cauldron purchase uses the taller green stacked background with `Unlock` above the coin icon and amount; all other cauldron action buttons hide while that slot is selected. Workshop `bag` and `stats` reuse the shared `45.5x80.25px` icon-over-label side-action contract: Bag uses `assets/game/source/icons/icon-bag.png` in the weighted left stack, while Stats uses `assets/game/source/icons/icon-stats-ledger-bag-style.png` as the first right-side action. Other purchase cost buttons stay green and tabs stay brown.
- Do not define mouse-hover states for row or button actions; press states must not tint row or button backgrounds.
- Underlines are allowed only for neutral selected options in mutually exclusive controls, such as tabs or one-of button panels; do not show them while held or pressed.
- Buy/place/claim prices stay right-aligned and use tabular numerals.
- Disabled or locked right-side actions use muted text and keep the same row height as enabled rows.

## Info Buttons

- Standalone info/help buttons use `assets/game/source/ui/prop_info.png` through the shared `setInfoButtonIcon` helper. Do not substitute `[i]`, `?`, or a font glyph.
- Callers keep ownership of the accessible `aria-label`, expanded state, tooltip/dialog action, and hit-target geometry. The shared helper owns only the visible icon and decorative-image semantics.
- Size the icon to the surrounding control: border-label info buttons use the border-label line height, while freestanding controls may use the full authored hit-target height.

## Lock Icons

- All visible lock iconography uses `assets/game/source/ui/prop_lock.png` through `createStatusIcon(..., STATUS_ICON_LOCK)`. Do not substitute CSS-drawn padlocks, Unicode locks, font glyphs, or alternate lock images.
- Keep meaningful player-facing `locked` text when it communicates state; this rule standardizes only the graphic.
- Callers continue to own the surrounding control's accessible label and geometry. Lock artwork remains decorative when the control or state label already exposes the locked state.

## Checkmark Icons

- All visible checkmark iconography uses `assets/game/source/ui/prop_checkmark.png` through `createStatusIcon(..., STATUS_ICON_CHECK)`. Do not substitute CSS-drawn checks, Unicode checks, native checkbox marks, font glyphs, or alternate checkmark images.
- Composite research icons that include a checkmark must source the same `prop_checkmark.png` artwork through `scripts/generate-research-icons.js`.
- Keep meaningful player-facing text such as `complete`, `claimed`, or `selected`; this rule standardizes only the graphic.
- Callers continue to own the surrounding control or state label's accessible name. The checkmark remains decorative when that name already communicates the state.

## Root Run Settings Controls

- `RootRunSettingsSliderPixi` in `src/rendering/pixi/primitives/PixiSettingsControls.js` is the shared settings slider. It uses a 14px progress rail so draggable allocation and settings controls read more clearly than passive 10px progress bars, plus the exact `assets/game/source/ui/root-run-settings/settings-knob.png` thumb at the documented 23px source size. `range` mode maps an integer `min`/`max`/`step` without visible milestones. `milestones` mode accepts ordered named options, renders dots only for interior stops, and snaps taps or horizontal drags to the nearest enabled option; the minimum and maximum remain selectable endpoints without redundant dots. Milestone fill roles are empty for `none`, red for `low`, yellow for `medium`, and green for `high`; mana uses blue. Disabled controls keep their full-opacity knob and geometry while rejecting press, drag, and semantic activation. Pointer input converts the input router's global point into local rail coordinates, blocks page-swipe capture, deduplicates unchanged writes, and exposes semantic activation.
- `RootRunSettingsTogglePixi` in `src/rendering/pixi/primitives/PixiSettingsControls.js` is the shared compact `60x24px` boolean switch matching the Root Run Settings dialog. It uses the same exact 23px knob asset as the settings slider, the Root Run brown capsule rail, and the lime inset for `on`. It supports `off`, `on`, pressed, and disabled states with immediate state changes and semantic activation. Feature rows own their label and locked copy; when a feature is locked, omit the toggle and show `locked` in the existing right-side status slot.
- `RootRunDevicePreferencesPanel` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved single light settings board inside the default white dialog paper. It uses the exact Root Run Settings nine-slice, `10px` board padding, and three `50px` source-pitch rows without nested cards or scrolling.
- `RootRunDevicePreferenceRow` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved icon-led settings row. It uses the exact Root Run sound, music, or vibration icon, a `19px` Lilita One uppercase label, and the shared `RootRunSettingsTogglePixi` in a fixed right-side slot. Supported states are `on`, `off`, pressed, and disabled; the current consumers are the device-local `sfx`, `music`, and `haptics` preferences.
- `DeviceIdentityFooter` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved settings identity footer below the board. It centers the real client version, renders a compact connected Spacetime identity while retaining the full value for copy, and places a yellow Root Run `copy` action directly beside the id. It supports unavailable, ready, pending, copied, and retry states.

## Percentage Allocation Lists

- Trader stall loaders use the exact Root Run Settings inner-panel backing as a nine-sliced background for every inventory row. Rows keep the Settings geometry: a 50px source pitch, 6px inter-row gap, and 8px left/right content padding.
- Tapping an inventory row selects one current item without moving stock. The selected row keeps resource/icon colors unchanged and shows the shared checkmark at the right edge with the same 8px inset.
- The allocation control is a progress rail with the exact Root Run Settings knob asset at 23px source size and `5%` steps from `0%` through `100%`. Do not show percentage labels under the rail. Percentages reconcile against matching stock already loaded plus matching stock still in inventory.
- `mark xN` applies the selected percentage in one gameplay update. `clear` returns loaded stock and stops future marking. `mark future` is a separate toggle that continuously routes newly produced copies to that stand without consuming stock already owned when enabled. Successful mark, clear, and future actions close the loader.
- A stand waiting for future production keeps its item target after loaded stock reaches zero and reads `waiting for <item>` instead of `empty stand`.

## Progress Rails

- All passive in-game progress, timer, tutorial, and scroll rails reuse the compact Root Rush geometry: 10px source height, a 1px black capsule border, a dark inset rim, a 1px inner gap, and fully rounded fill caps.
- Interactive sliders and the top-panel quest rail use 14px source height. Sliders preserve the shared capsule geometry and existing knob size.
- The default fill is purple `#8740df` with a lighter purple `#bd72f3` inset edge. Brewing overrides it with blue, Garden with green, and Market/Research with yellow. Trader-stall sale progress is the Market exception: it stays purple and does not inherit the gradient player style. The top-panel level rail uses Root Run's image-backed rounded track, a fixed Idle Wizard purple-to-gold gradient independent of the player's selected rail style, thin request separators, and no visible progress numbers. App-level connection/loading rails keep the selected gradient treatment. Alternate researched fill treatments keep the same rail geometry.
- Reveal progress by changing width or the live right edge. Never use `scaleX`, because it compresses the rounded caps.

## Expandable Boxes

- Collapsed boxes keep one full summary row visible inside the box.
- Use a top-right border label for count or progress, such as `2/5`.
- Use a bottom-center border label as the toggle: `expand` when collapsed, `collapse` when open.
- Expand hidden rows in normal flow inside the same box, without overlaying nearby panels.
- Workshop main progression uses one always-open `Elara's Request` box. Show only the active request; the shared top panel owns the approved HUD Level Rail with one segment per configured request and no progress-number caption. Request completion reuses Root Run's exact research-XP feedback contract in `PixiTopPanelView`: one 68px Root Run source star from the request surface to the level star, a distance-timed direct 96px source arc, progress held until arrival, an eight-spark arrival burst, and a 400ms destination impact. Retained Pixi maps those measurements through the shared `/3` source scale. Reduced motion skips directly to the completed state. Completing the final request automatically advances the player level; never relabel the request box or add a separate level-up action. Keep the whole progress row hidden at level 0 and reveal it after the automatic transition reaches level 1. Do not add XP copy, an expandable checklist, pinning, or task reordering.

## Popup Structure

- Default dialog titles sit centered in the separate Root Run purple title plaque above the brown shell; ordinary box titles remain embedded border titles.
- Dismissible dialogs use the shared round Root Run X asset centered below the shell at the authored gap; retain its accessible close label. Blocking dialogs use the same shell with the close action hidden.
- Back, previous, and next actions remain text controls. Never reuse a close class for navigation or replace navigation text with the X asset.
- Tabbed popups put tabs below and outside the bordered dialog, with the same stroke as the popup.
- True tab buttons use the shared selected-tab state so every tab has explicit selected and unselected views. Text tabs use the very-dark Root Run brown button skin when deselected and the lighter brown skin when selected; the five bottom room icon tabs alone use the full-width midnight dark/lighter top-cap nine-slice row with an `8px` bottom safe gap. Do not assign tab semantics to previous/next pager buttons.
- Standard tabbed popups use `--style-tabbed-dialog-width` on the panel and `260px` content width on the dialog. Wider `286px` dialogs must use the wider panel token too; never mix a standard tab panel with a wider dialog.
- Tabbed dialogs keep a fixed bordered panel height; overflowing tab content scrolls inside that panel while the bottom tabs remain fixed.
- Every scrollable dialog pane puts `style-page-scroll` on the actual scrolling viewport. Wheel and drag input use the station resistance/inertia/spring model; the track/thumb appears at the right edge only when content overflows and the thumb compresses during elastic top/bottom overscroll. Never reserve bottom space or add a sibling scroll-progress rail.
- Popup rows should keep stable DOM nodes during snapshot updates so mobile taps do not miss after touchend.
- Book-like catalogues use compact column headers and stable selectable rows; selecting a row reveals a short detail/history block in the same dialog instead of stacking another catalogue modal.
