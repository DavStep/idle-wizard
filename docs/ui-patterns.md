# UI Patterns and Reusable Widget Library

This is the authoritative reusable widget library for Idle Wizard. Before adding UI, map every visible or interactive building block to an entry here and reuse its contract.

## Reuse Rule

- Classify each building block as `reuse`, `extension`, or `new widget` before editing product UI code.
- `reuse` uses a library entry unchanged. `extension` supplies feature data, copy, or composition without changing the entry's visual, interaction, state, or accessibility contract.
- `new widget` includes a new primitive, compound component, scroll behavior, box/dialog type, control pattern, or a meaningfully different variant of an existing entry.
- Before adding a new widget, identify its exact name, purpose, closest existing entry and why it is insufficient, supported states, and intended consumers.
- Implement new widgets as reusable project components and add them to this library with their stable source, contract, and real-app preview evidence.
- Every completed UI task reports `New widgets: none` or lists the widgets introduced.

## Current Library

| Widget | Stable source | Contract and approved use |
| --- | --- | --- |
| EditorWorkspaceBar | `UiEditorWorkspaceManager` in `src/uiEditor/managers/UiEditorWorkspaceManager.js`; `.ui-editor-toolbar` in `src/uiEditor/uiEditor.css` | Development-only global editor bar above the docked workspace. It exposes one native `Save workspace` action plus the matching `Ctrl+S` / `Command+S` shortcut, saves a versioned browser-local snapshot of dock sizes, open library folder, selected preview, and hierarchy visibility overrides, and restores the latest valid snapshot on editor mount. A compact polite live status reports restored, saved, unavailable, and error outcomes without blocking the editor. States are idle, restored, saved, unavailable, error, hover, focused, pressed, and narrow. Scope is the standalone UI editor. Evidence: `screenshots/ui-editor-workspace-save-390x844.png` and `screenshots/ui-editor-workspace-save-native-crop.png`. |
| EditorComponentHierarchy | `UiEditorHierarchyManager` in `src/uiEditor/managers/UiEditorHierarchyManager.js`; `.ui-editor-hierarchy` in `src/uiEditor/uiEditor.css` | Development-only semantic tree that mirrors the element hierarchy of the widget or dialog mounted in the standalone editor preview. Retained Pixi previews bridge meaningful virtual children into the same tree, including button background, label, icon, portrait, and compound cost-label parts, without exposing internal canvas or nine-slice quads. Rows preserve nesting with `tree`/`treeitem`/`group` semantics, show the component label plus element or atomic type, and provide a leading native eye button for every component. Atomic rows are pointer- and keyboard-selectable and use the shared selected-row surface. The eye applies a reversible editor-only visibility change, never removes the component, and exposes `Hide …` / `Show …` labels plus pressed state. Parent-hidden descendants remain in the tree and render muted. States are empty, populated, visible, selected, editor-hidden, ancestor-hidden, overflowing, and narrow. Scope is the editor left panel. Evidence: `screenshots/ui-editor-hierarchy-1280x900.jpg`, `screenshots/ui-editor-hierarchy-native-crop.jpg`, `screenshots/ui-editor-hierarchy-390x844.jpg`, `screenshots/ui-editor-atomic-components-1280x900.jpg`, and `screenshots/ui-editor-atomic-components-390x844.jpg`. |
| EditorWidgetInspector | `UiEditorUsageManager` in `src/uiEditor/managers/UiEditorUsageManager.js`; `.ui-editor-usages` in `src/uiEditor/uiEditor.css` | Development-only right-panel inspector for a selected widget, asset, atlas frame, or retained atomic component. Widget summaries expose resolved font and production background metadata before dense feature usage rows; asset summaries expose their registered production metadata and consumers. Atlas-frame selection replaces the atlas summary with frame ID, source path, packed and source sizes, atlas coordinates, footprint, and atlas ID. Atomic selections replace those read-only sections with native local-position number fields plus text or compatible background-asset controls when supported, applying edits immediately to the mounted preview without mutating production sources. Assetless and image-only controls use explicit `None` values. States are unselected, widget, asset, atlas frame, atomic text, atomic image, no registered usages, invalid numeric input, overflowing, and narrow. Scope is the standalone UI editor. Evidence: `screenshots/ui-editor-widget-inspector-1280x900.jpg`, `screenshots/ui-editor-widget-inspector-native-crop.jpg`, `screenshots/ui-editor-widget-inspector-390x844.jpg`, `screenshots/ui-editor-atomic-components-1280x900.jpg`, and `screenshots/ui-editor-atomic-components-native-crop.jpg`. |
| EditorAssetWorkbench | `createUiEditorAssetPreview` in `src/uiEditor/widgets/UiEditorAssetWorkbench.js`; `.ui-editor-asset-workbench` in `src/uiEditor/uiEditor.css` | Development-only central preview router for every source texture in the production manifest. Ordinary images render at contained natural aspect over a transparency checkerboard; eligible PNGs expose one inline `Convert to 9-slice` action, while registered or saved nine-slices delegate directly to `EditorNineSliceWorkbench`. Asset workbenches collapse the component-hierarchy dock because their internal DOM is not an editable scene, then restore its prior width when a widget, dialog, or scene opens. WebP and non-source assets remain preview-only. States are image loading, image ready, image error, conversion available, conversion unavailable, nine-slice delegated, overflowing, and narrow. Scope is the standalone UI editor asset catalogue. Evidence: `screenshots/ui-editor-assets-1280x900.jpg` and `screenshots/ui-editor-assets-390x844.jpg`. |
| EditorAtlasWorkbench | `createUiEditorAtlasWorkbench` in `src/uiEditor/widgets/UiEditorAtlasWorkbench.js`; `.ui-editor-atlas` in `src/uiEditor/uiEditor.css` | Development-only generated-atlas inspector that keeps the packed texture spatially intact while making every registered frame discoverable. Pointer hover shows the frame under the cursor; release selects its exact packed bounds and projects frame ID, source path, packed size, original source canvas, atlas coordinates, footprint, and atlas ID into `EditorWidgetInspector`. Search matches both frame ID and source path, outlines every match in place, and scopes Previous/Next, Enter, and canvas-arrow traversal. Home/End jump within the current match set, Escape clears selection, and selection is kept visible when the viewport is zoomed. Explicit Copy ID and Copy path actions use a polite result status. Fit width is the default; stepped zoom and `100%` support pixel inspection through the same overflow viewport. States are loading, ready, image error, hovered, selected, no selection, searching, no matches, match set, fitted, zoomed, copied, copy error, overflowing, keyboard focused, narrow, and reduced-motion. Scope is preview-only generated atlases in the standalone UI editor. Reproducible preview: open `UI Assets / Generated / Atlases / game atlas`. Evidence is recorded by the atlas-viewer QA captures. |
| EditorAssetDeletionDialog | `createUiEditorAssetDeletionDialog` in `src/uiEditor/widgets/UiEditorAssetDeletionDialog.js`; `inspectUiEditorAsset` and `deleteUiEditorAsset` in `scripts/ui-editor-nine-slice-plugin.mjs`; `.ui-editor-asset-delete` in `src/uiEditor/uiEditor.css` | Development-only native modal review for safely removing a production source texture. It shows one selected-asset summary, registered widget previews with their real feature locations, exact scanned project references with file/line/excerpt evidence, and a searchable visual replacement gallery limited to the same extension and image/nine-slice type. Used assets keep `Replace references and delete` disabled until inspection succeeds and one replacement is selected; unused assets may use `Delete asset` directly. Confirmation rechecks the source state, rewrites every reviewed path representation, deletes the texture plus its optional sidecar, removes the stale library entry, and opens the replacement. Runtime and generated textures remain read-only. States are inspecting, inspection-error, no visual usages, visual usages, no references, referenced, filtering, no replacement matches, replacement-selected, deleting, delete-error, deleted, overflowing, narrow, and reduced-motion. Scope is local UI asset maintenance. Reproducible preview: `ui-editor.html?preview=asset-deletion`. Evidence: `screenshots/ui-editor-asset-deletion-1280x900.png`, `screenshots/ui-editor-asset-deletion-390x844.png`, `screenshots/ui-editor-asset-deletion-native-crop.png`, and `screenshots/ui-editor-asset-deletion-reference-review.html`. |
| EditorNineSliceWorkbench | `createUiEditorNineSliceWorkbench` in `src/uiEditor/widgets/UiEditorAssetWorkbench.js`; `.ui-editor-nine-slice` in `src/uiEditor/uiEditor.css` | Development-only compound editor for inspecting and stress-testing production nine-slices. Its stable left pane owns the source, direct left/top/right/bottom fields, and draggable keyboard-operable constrained guides. The right pane uses two exclusive tabs instead of one shared preview scroller. `Preview cases` renders seam-free canvas results in a fixed two-column matrix: Original above Height stretched on the left, Width stretched above Both stretched on the right. Original always uses the calculated minimum renderable width and height from the active output borders; the three stress cases derive from that minimum. These four cases auto-fit and accept no zoom or pan. `Custom testing` owns synchronized range and exact-number dimensions, optional ratio locking, zoom, fit, mouse/touch panning, and arrow-key panning. Reset, Copy CSS, live CSS output, and polite result status remain shared. Opposing guides cannot cross and invalid numeric input is clamped to the current legal range. Editable source nine-slices expose the existing authoring save action, while runtime aliases and generated assets stay preview-only. States are source loading, source error, preview-cases-selected, custom-testing-selected, custom-resized, ratio-locked, custom-zoomed, custom-panned, guide-hovered, guide-focused, guide-dragging, margin-edited, saving, saved, validation-error, server-error, copied, copy-error, overflowing, and narrow. Scope is the standalone UI editor asset catalogue. Evidence: `screenshots/ui-editor-nine-slice-preview-layout-1280x900.png`, `screenshots/ui-editor-nine-slice-preview-layout-native-crop.png`, and `screenshots/ui-editor-nine-slice-preview-layout-reference-review.html`; minimum sizing: `screenshots/ui-editor-nine-slice-minimum-original-1280x900.png`, `screenshots/ui-editor-nine-slice-minimum-original-native-crop.png`, and `screenshots/ui-editor-nine-slice-minimum-original-reference-review.html`. |
| EditorTabs | `createEditorTabs` in `src/uiEditor/widgets/UiEditorAssetWorkbench.js`; `.ui-editor-tabs` in `src/uiEditor/uiEditor.css` | Development-only compact two-or-more mode switcher for mutually exclusive editor surfaces. It uses native buttons with `tablist`/`tab`/`tabpanel` semantics, exactly one selected and tabbable tab, click selection, and Left/Right/Home/End keyboard navigation with focus following selection. Hidden panels leave layout and accessibility flow. States are idle, hover, focused, selected, and narrow. Its first consumer separates nine-slice fixed preview cases from custom testing; it does not represent player-facing game tabs. Evidence: `screenshots/ui-editor-nine-slice-preview-layout-1280x900.png` and `screenshots/ui-editor-nine-slice-preview-layout-390x844.png`. |
| EditorPanZoomViewport | `createPanZoomViewport` in `src/uiEditor/widgets/UiEditorAssetWorkbench.js`; `.ui-editor-pan-zoom-viewport` in `src/uiEditor/uiEditor.css` | Development-only editor canvas viewport that auto-fits its content, then layers `50%` through `400%` user zoom and clamped two-axis pan without moving surrounding layout. Pointer/touch drag pans until the matching window-level release or cancellation; focused arrow keys pan, Shift increases the step, and Home resets pan. A companion control row provides zoom out, percentage status, zoom in, and Fit. States are fitted, zoomed, panned, actively panning, keyboard-focused, and reduced-motion. Its first and intended consumer is the nine-slice custom-size tester; fixed cases and the source slicer explicitly do not use it. Evidence: `screenshots/ui-editor-nine-slice-preview-layout-1280x900.png` and `screenshots/ui-editor-nine-slice-preview-layout-390x844.png`. |
| EditorNineSliceAuthoring | `createUiEditorAssetPreview` and `saveUiEditorNineSlice` in `src/uiEditor/widgets/UiEditorAssetWorkbench.js`; `createUiEditorNineSlicePlugin` in `scripts/ui-editor-nine-slice-plugin.mjs`; `.ui-editor-asset-workbench__actions` in `src/uiEditor/uiEditor.css` | Development-only authoring state that creates or updates a production source PNG's nine-slice definition from the existing workbench. `.9.png` is the authoritative asset-type suffix and opens directly as a nine-slice. Conversion preserves source pixels, promotes the filename to `.9.png`, updates project references, seeds margins from a registered sibling when available, otherwise starts from safe quarter margins, and exposes `Cancel` plus `Save 9-slice`; an existing source nine-slice exposes `Save 9-slice` directly. Save validates that both axes retain a stretchable pixel, then writes `<asset>.9slice.json` beside the renamed PNG through the local development server. States are available, unavailable-non-PNG, draft-loading, draft-ready, editing, saving, saved, validation-error, server-error, name-conflict, and narrow. Scope is local UI asset authoring. Evidence: `screenshots/ui-editor-convert-asset-1280x900.png`, `screenshots/ui-editor-nine-slice-authoring-1280x900.png`, `screenshots/ui-editor-nine-slice-authoring-390x844.png`, `screenshots/ui-editor-green-button-short-9slice-qa.png`, and `screenshots/ui-editor-nine-slice-conversion-reference-review.html`; first saved asset is `green-button-short.9.png` at `L85 T100 R43 B68`. |
| EditorAssetFilter | `UiEditorBottomPanelManager` in `src/uiEditor/managers/UiEditorBottomPanelManager.js`; `.ui-editor-library-filter` in `src/uiEditor/uiEditor.css` | Development-only native search field aligned to the far right of the asset-folder breadcrumb header. It matches filename or production asset path without changing the selected entry, reports visible and total counts through a polite status, and keeps lazy thumbnails inside the existing gallery. The count hides at narrow widths so the breadcrumb and search remain usable in the single header row. States are empty query, matching, no matches, focused, overflowing, and narrow. Scope is the standalone editor asset catalogue. Evidence: `screenshots/ui-editor-header-search-desktop.png`, `screenshots/ui-editor-header-search-390x844.png`, `screenshots/ui-editor-header-search-native-crop.png`, and `screenshots/ui-editor-header-search-reference-review.html`. |
| EditorFolderBrowser | `UiEditorBottomPanelManager` in `src/uiEditor/managers/UiEditorBottomPanelManager.js`; `.ui-editor-folder-browser`, `.ui-editor-folder-grid`, and `.ui-editor-folder` in `src/uiEditor/uiEditor.css` | Development-only Codex-style hierarchical library browser for the bottom editor dock. Its `Library` root contains `UI Assets`, `UI Widgets`, `Dialogs`, and `Scenes`; `UI Assets` deduplicates every source texture in the production asset manifest and uses `EditorAssetFilter` for discovery. Source assets generally mirror production directories, while reusable visual roles may use semantic editor-only folders: `UI/Banners` holds shared title plaques and ribbons, `UI/Backgrounds` holds related paper, card, and inner-panel nine-slices, and `UI/Currencies` holds the canonical coin, crystal, emerald, mana, and ruby icons. Background entries expose their geometry family and visual variant, but retain asset-specific slice and minimum-size compatibility contracts. Asset IDs and production paths remain unchanged. `UI Widgets` contains `Buttons`, `Progress bars`, `Sliders`, and `Composite widgets`. Native release-only folder buttons use a compact outline folder icon and a responsive flat grid, while the panel header exposes an accessible breadcrumb path for returning to parent folders. Folder visits keep browser-style session history through `Command+[` / `Command+]`, `Alt+Left` / `Alt+Right`, and dedicated browser navigation keys; a new visit after moving back replaces the forward path, while editable fields and open dialogs retain their own keys. Leaf folders show their stable library-entry buttons without decorative empty-state copy. States are root, nested, history-back, history-forward, branched history, empty leaf, populated leaf, hover, focused, overflowing, and narrow; disabled/loading/error and motion states do not apply. Scope is the standalone UI editor, with room for future editor catalogues that use the same folder tree contract. Evidence: `screenshots/ui-editor-folder-browser-1280x900.jpg`, `screenshots/ui-editor-folder-browser-native-crop.jpg`, `screenshots/ui-editor-folder-browser-390x844.jpg`, `screenshots/ui-editor-folder-browser-reference-review.html`, `screenshots/ui-editor-banners-1280x900.jpg`, `screenshots/ui-editor-banners-native-crop.jpg`, and `screenshots/ui-editor-banners-reference-review.html`. |
| EditorLibraryEntry | `UiEditorBottomPanelManager` in `src/uiEditor/managers/UiEditorBottomPanelManager.js`; `.ui-editor-library-entry` in `src/uiEditor/uiEditor.css` | Development-only native button representing one widget, asset, dialog, or scene factory. Release selects the entry and updates the central preview plus component hierarchy without rebuilding catalogue controls. Compatible retained button entries reuse the mounted canvas, renderer, projection, and background while replacing only the widget display object; incompatible widget/asset/dialog/scene types mount a fresh preview surface. Exactly one entry may expose `aria-pressed="true"` across the catalogues. Entries with a thumbnail factory use a compact wrapping gallery tile: a passive production-rendered preview sits above a centered label, while the complete native button remains the only library selection target. Asset thumbnails render the source image directly and mark nine-slice entries with a compact `9` badge. Preview rendering is visibility-managed so scrolling the widget catalogue does not retain an application for every offscreen tile. The default `Buttons` folder registers the retained `PixiButton`, `PixiCostButton`, `PixiInfoButton`, HUD settings, and HUD avatar contracts with their production artwork; deterministic dialog and scene entries remain available through the `library-selection` QA fixture. States are idle, hover, focused, selected, disabled, thumbnail-loading, thumbnail-ready, thumbnail-error, overflowing, and narrow. Scope is populated editor library sections. Evidence: `screenshots/ui-editor-button-thumbnails-1280x720.jpg`, `screenshots/ui-editor-button-thumbnails-native-crop.jpg`, `screenshots/ui-editor-button-thumbnails-reference-review.html`, and `screenshots/ui-editor-assets-1280x900.jpg`. |
| EditorDockPanel | `UiEditorViewManager` in `src/uiEditor/managers/UiEditorViewManager.js`; `.ui-editor-panel` in `src/uiEditor/uiEditor.css` | Development-only Codex-style dock panel below the global workspace bar. Left and right instances run the remaining editor height; the bottom instance sits under the preview between them. Each has a compact `34px` header and an independently overflowing body. The defaults are `260px` left, `300px` right, and `220px` bottom, with responsive clamps that preserve a central preview on narrow windows. States are empty, populated, overflowing, resized, and narrow. Scope is the standalone UI editor. Evidence: `screenshots/ui-editor-docked-shell-1280x900.jpg`, `screenshots/ui-editor-docked-shell-native-crop.jpg`, `screenshots/ui-editor-docked-shell-390x844.jpg`, and `screenshots/ui-editor-docked-shell-reference-review.html`. |
| EditorResizeSplitter | `UiEditorPanelLayoutManager` in `src/uiEditor/managers/UiEditorPanelLayoutManager.js`; `.ui-editor-splitter` in `src/uiEditor/uiEditor.css` | Development-only `5px` dock resize handle with a centered `1px` neutral divider. Pointer and touch drags may begin only on the handle, then remain tracked through window-level movement and release so retargeting cannot leave resize mode stuck. Focused splitters use arrow keys in `8px` steps or Shift plus arrow in `32px` steps, expose separator orientation and current/min/max values, preserve a minimum preview, and use the editor accent only for hover, focus, or active resizing. Reduced motion removes the divider transition. Evidence: `screenshots/ui-editor-docked-shell-1280x900.jpg`, `screenshots/ui-editor-docked-shell-native-crop.jpg`, `screenshots/ui-editor-docked-shell-390x844.jpg`, and `screenshots/ui-editor-docked-shell-reference-review.html`. |
| Backend Loading Splash | `PixiLoadingSplash` in `src/rendering/pixi/global/gates/PixiLoadingSplash.js` | Full-screen, non-interactive backend boot and reconnect surface using the Idle Wizard splash art with the exact wooden-sign title `Idle Wizard`, fitted to the authored game width at its original aspect ratio and top-aligned so vertical overflow is clipped. Wide-stage gutters remain on the shared dark background; the splash art, loading label, and gradient progress rail stay inside and centered to the authored game bounds. It supports loading progress, a completed held state while connection is still pending, and an immediate completed rail under reduced motion. Connection failures remain in the loading state; actionable account-in-use and maintenance states use the default player dialog instead. Intended consumers are native launch and the retained online gate. Evidence: `screenshots/loading-splash-game-bounds-390x844.png`, `screenshots/loading-splash-game-bounds-native-crop.png`, `screenshots/loading-splash-game-bounds-desktop-1280x900.png`, and `screenshots/loading-splash-game-bounds-review.html`. |
| Ordinary border-labeled box | `.style-box`, `.style-box__title` in `src/styles/base.css` | Default room section with compact padding, embedded transparent title, no dialog shadow. |
| Expandable inventory box | `src/pages/shared/RoomInventoryBoxManager.js` | Ordinary box with count label, collapsed summary rows, and bottom-border expand/collapse action. |
| Default player dialog shell | `PixiDialogFrame` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; `.style-dialog`, `.style-box__title` in `src/styles/base.css` for fallback markup | Root Run Expedition brown outer frame with a fixed `304px` base content width and `53px` minimum content height, paper content, purple title plaque, detached round close control centered below the shell when dismissal is allowed, and modal shadow. Named wider or fixed-height variants may override the base geometry. All game dialogs, including blockers and the first tutorial story prompt, use this shell. |
| Split-paper dialog composition | `createDialogPaperSection`, `resolveDialogPaperOutsets`, and `setDialogPaperSectionBounds` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; first composition in `ShopDialogPixi` | Reuses the exact Expedition paper nine-slice for two or more full-width sections inside one brown dialog shell. Hide the shell's continuous paper, keep content on the standard dialog column with no feature-local side inset, expand each paper frame through the shared `20px` dialog padding, and preserve an `8px` visible paper-to-paper gap. Section content begins `5px` below its content boundary; use the shared row pitch and feature-owned row gap. A tabbed split-paper dialog ends its final paper section at the shared footer-tab boundary; Level rewards reuses the split-paper composition without tabs. |
| Level Rewards Dialog | `PixiLevelDialog` in `src/rendering/pixi/global/dialogs/PixiLevelDialog.js`; `.room-top-panel__level-dialog--board-backed` in `src/styles/base.css` | Feature composition of the default player dialog shell and the existing split-paper pattern. The gained and total groups each use a full-width Expedition paper section, while fixed yellow previous/next buttons sit inside the lower board corners. The pager supports previous-hidden, both-visible, and next-hidden states without shifting its side anchors. All labels use Title Case and resource amounts keep their icons. |
| Danger Dialog Title Plaque | `PixiDialogFrame` with `titleVariant: 'danger'` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; `.style-dialog--danger` in `src/styles/base.css` for fallback markup | Approved destructive or loss-bearing title variant. It preserves the default Expedition plaque geometry, ornaments, white outlined title text, dialog shell, close behavior, and motion while hue-shifting only the purple plaque art to semantic red. It has no selected, disabled, loading, error, empty, overflow, or separate reduced-motion state. First consumer: Garden `Cancel Progress?`. Evidence: `screenshots/garden-cancel-danger-390x844.png`, `screenshots/garden-cancel-danger-native-crop.png`, `screenshots/garden-cancel-danger-desktop-fitted.png`, and `screenshots/garden-cancel-danger-reference-review.html`. |
| Full-screen announcement screen | `PixiAnnouncementSurface` in `src/rendering/pixi/global/dialogs/PixiMessageDialogs.js`; `.room-announcement-layer` in `src/styles/base.css` | Centered, unframed progress, research, level-up reward, and feature-unlock presentation over the full-screen backdrop. Level-up and research-complete celebrations reuse the Market ribbon asset for their Title Case heading plus dark retained rows with left white outlined labels and right white values. Research Complete centers the existing large research atlas icon between the ribbon and rows, using the single-item announcement icon geometry without duplicating the research/effect labels around it. Level-up rows hold each earned delta such as `+50`, then count from the previous authoritative total to the new total before revealing `Tap to continue`; research-complete rows preserve their normal timed dismissal. Reduced motion settles directly on the final presentation. Preview screens dismiss from the backdrop and do not show a dialog close control; report-style announcements opt into the default dialog shell. |
| Base text button | `.style-button` in `src/styles/base.css`; `PixiButton` in `src/rendering/pixi/primitives/PixiButton.js` | Shared compact button foundation; feature classes may size or place it without inventing new state treatment. Room-level text tabs and compact room actions use the shared `36px` source height, while dialog footer tabs retain their separate `28px` geometry. |
| Regular button color variants | `.style-button--yellow`, `.style-button--green`, `.style-button--red`, `.style-button--brown-dark`, `.style-button--brown-light` in `src/styles/base.css`; `PixiButton` in `src/rendering/pixi/primitives/PixiButton.js`; PNG matrix in `assets/game/source/ui/regular-button` | Shared Root Run regular-button geometry. The asset family contains blue, brown, dark-brown, gray, green, purple, red, and yellow, each with `50`, `30`, and `15` squircle-radius tiers. Their source slices are `L86 T100 R52 B68`, `L52 T60 R32 B41`, and `L27 T30 R16 B20`, producing minimum source sizes of `141x171`, `87x104`, and `46x53`. Each includes the bottom shadow, a flat `3x3` stretch center, and one matching flat gutter pixel around every center edge so smoothed rendering cannot pull decoration or curved-cap pixels into a join. Existing regular actions and cost-button compositions use radius `50`. Choose color by role: green for positive apply/mark and affordable costs, red for enabled cancel/clear actions, yellow for highlighted or completed actions, purple for quest actions, and brown or dark-brown for neutral roles. Disabled variants swap to gray artwork without a grayscale or monochrome shader. Account Save reuses the green confirm variant. Popup tabs reuse the brown, dark-brown, and gray radius-50 textures while retaining their own compact output geometry and the same button input, semantic, press, and disabled contracts. |
| Cost button | `src/pages/shared/CostButtonManager.js`, `.style-cost-button`; `PixiCostButton` in `src/rendering/pixi/primitives/PixiCostButton.js` | Price/cost composition with icon-before-value presentation, unaffordable and disabled states, accessible label, and isolated click handling. Background states reuse the canonical regular-button nine-slices, and coin content reuses `assets/game/source/icons/icon-coin.png`, exposed in the editor under `UI/Currencies`. |
| Stacked Cost Button | `PixiCostButton` with `stacked: true` in `src/rendering/pixi/primitives/PixiCostButton.js` | Approved retained purchase variant for a compact explicit action above an icon-and-amount row. It nine-slices the shared regular-button green, purple/blue, and gray assets at `92x52`, requires both `actionLabel` and `amountLabel`, and preserves the base cost button’s available, unaffordable, disabled, press, focus, semantic-target, and callback contracts. No selected, loading, error, empty, or decorative-motion variants. First consumer: the Brewing locked-cauldron `Unlock` action. |
| Base input | `.style-input` in `src/styles/base.css` | Shared text, number, textarea, and select foundation with normal, focus, placeholder, and disabled states. |
| PixiTextField: brown-inset | Default `PixiTextField` skin in `src/rendering/pixi/primitives/PixiTextField.js`; explicit `variant: 'brown-inset'` remains supported | Approved default retained text-entry treatment for message composers, usernames, profile or guild naming, feedback, numeric entry, and other player-authored inputs. It uses `assets/game/source/ui/root-run-world-chat/world-chat-text-field.9.png` with symmetric `26px` source slices, cream value/caret text, muted warm placeholder copy, selection tint, and a restrained gold focus inset. Consumers vary only dimensions, input kind, placeholder, and behavior; do not introduce feature-local input chrome. Use explicit `variant: 'control'` only when an approved specialized surface requires the legacy frame. The Root Run Account composition alone uses `account-username` to place the same native text-entry behavior transparently over its exact username-row raster with authored white outlined type. It preserves native text-entry, max-length, submit, cancel, change, keyboard-inset, focus, multiline, and disabled-by-caller contracts. First consumer: the World Chat composer. Visual evidence: `screenshots/world-chat-text-field-390x844.png`, `screenshots/world-chat-text-field-native-crop.png`, `screenshots/world-chat-text-field-desktop-fitted.png`, and `screenshots/world-chat-text-field-reference-review.html`. |
| Managed vertical scroll pane | `.style-page-scroll`, `src/pages/managers/ScrollCueManager.js`; `RetainedScrollArea` and `resolveRetainedDialogListLayout` in `src/rendering/pixi/pages/workshop/RetainedPageKit.js` | The only scroll behavior. Use it for full room pages, dialog/popup content, and bounded lists; context changes sizing, not physics or scrollbar type. The retained scrollbar is Root Run's procedural Pixi station component: antialiased `Graphics.roundRect` track and thumb, exact `/3` geometry and palette, progressive asymmetric overscroll, proportional thumb compression, fixed `0.68` release filtering, and `520`/`26` edge return. Standard retained dialog lists inset image-backed row frames by `8px` on each side, keep the feature-owned row pitch and gap, add a `2px` viewport allowance before the shared track gap, and place the overflow-only scrollbar in the reserved right paper gutter. Reproducible visual harness: `src/dev/uiRecipes/retained-scrollbar-parity.html`. |
| Progress rail | `.style-progress`, `.style-progress__fill`, `src/pages/shared/progressFill.js` | Shared timer/progress geometry and width-driven fill with reduced-motion handling; use approved room color roles only. |
| Timed Progress Rail | `PixiTimedProgressBar` in `src/rendering/pixi/primitives/PixiProgressBar.js`; `RetainedTimedProgressBar` in `src/rendering/pixi/pages/workshop/RetainedPageKit.js` | Retained extension of the shared Progress Rail for elapsed-time work. Its complete timer state is only `startedAtMs` and `durationMs`; it derives fill, completion, and remaining time from the current clock on every active render tick. Gameplay remains authoritative: feature snapshots are converted once at the binding boundary, backward authoritative progress restarts the timer, and low-frequency snapshots never drive the visible fill frame by frame. Supports idle, running, complete, and restarted states. First consumers: Brewing batches, Garden plots, and Research work. Evidence: `screenshots/garden-progress-smooth-390x844-a.png`, `screenshots/garden-progress-smooth-390x844-b.png`, and `screenshots/researching-status-390x844.jpg`. |
| Amount selection row | `src/pages/shared/AmountSelectionRow.js` | Shared decrement/value/increment control with editable numeric value and accessible step labels. |
| Numbered or inline-action row | `Numbered Rows` and `Inline Row Actions` below | Compact left marker, middle content, fixed right action/status rhythm. |
| Research station-upgrade row | `ResearchRowWidget` in `src/rendering/pixi/pages/research/ResearchPixiPage.js`; `.research-page__row` in `src/styles/base.css` | Compact `80px` research catalogue row using the shared Root Run station-upgrade card, fitted between the shared content edges so the scroll viewport and right-edge scrollbar cannot clip it, with a scaled left art well, an adaptive middle description, and a scaled right cost/status control. Single-level research has no level badge. Levelled research places the shared star rank directly after its name: two total levels use two slots, while three or more total levels cap the display at three slots; the current rank fills the available slots using the shared star-tone progression. The description uses the full fixed middle region and keeps the normal `11px` source size when possible, shrinking toward `8px` only when its wrapped height would overflow. Its measured vertical center includes a `-10px` optical correction so Lilita One's visible glyphs, rather than its low-sitting line box, center against the art and action. Locked rows reuse the normal card, art well, artwork, and ink under a `30%` black overlay; completed rows show `Researched` on the shared yellow regular-button nine-slice. The title and description share the row ink color. |
| Tintable white squircle skin | `assets/game/source/ui/white-squircle`; `PIXI_SQUIRCLE_TINTS` and `PIXI_ROOT_RUN_ASSETS.researchArt` in `src/rendering/pixi/theme/PixiThemeTokens.js` | Shared solid squircle mask family for art wells and other flat semantic fills. Radius families `2` through `114` stay white and preserve their imported alpha silhouette; consumers tint the selected size in Pixi or use it as a CSS mask instead of adding color-baked PNG variants. The radius-40 texture currently backs Research, Prestige, Market, and Brewing art wells. Disabled or locked color remains consumer-owned state, and callers retain their existing output insets and geometry contracts. |
| Research Station Title Plaque | `ResearchStationTitlePlaque` in `src/rendering/pixi/pages/research/ResearchPixiPage.js`; `.research-page__box-title` in `src/styles/base.css` | Static section heading using the Root Run `header-red` horizontal nine-slice geometry with semantic color variants: Regular is yellow, Automation is red, Advanced is emerald green, Crystal is purple, and Brewing is mana blue. It connects directly to the screen's left edge at `42px` source height while inset content keeps its normal `16px` edge. It uses a single-line Title Case `18px` Lilita One label and may reserve one inline trailing slot for a compact passive status such as the shared three-star rank; the plaque measures both pieces plus the tightened decorated right-cap allowance. Width caps at the right content edge; only an overlong label may reduce toward the normal `13px` source size. The visible title still names the section, so color is not the only identifier. No selected, disabled, loading, error, empty, or motion variants. Approved Research evidence: `screenshots/research-title-variants-native-contact-sheet.png`, the four `screenshots/research-title-*-390x844.png` authored views, and `screenshots/research-title-variants-desktop-fitted.png`. Brewing evidence: `screenshots/brewing-cauldron-title-blue-390x844.png`, `screenshots/brewing-cauldron-title-blue-native-crop.png`, and `screenshots/brewing-cauldron-title-blue-desktop-fitted.png`. |
| Market Title Ribbon | `MarketTitleRibbon` in `src/rendering/pixi/pages/shop/MarketTitleRibbon.js` | Static Market licence heading using the approved purple folded-ribbon PNG as a horizontal nine-slice with fixed authored height and `73 27 73 27` source margins. The ribbon alone may stretch while its tails, folds, and outline remain fixed. The Title Case licence name and all three rank-star slots form one measured group centered horizontally on the ribbon front and shifted `6px` upward at the authored `390px` surface to align with the raised panel rather than the lower folds. The ribbon has no separate jewel or left-side decoration. Overlong names shrink toward the normal `13px` source floor while the star geometry stays fixed. Market is the only consumer for now. No selected, disabled, loading, error, empty, or motion variants. Visual evidence: `screenshots/market-title-ribbon-fixed-390x844.jpg`, `screenshots/market-title-ribbon-fixed-native-crop.jpg`, and `screenshots/market-title-ribbon-fixed-desktop-fitted.jpg`. Stretch evidence: `screenshots/market-title-ribbon-9slice-qa.png`. |
| Market Offer Row | `MarketOfferRow` in `src/rendering/pixi/pages/shop/ShopPixiPage.js` | Crystal-tab offer card derived from the retained Traders stall composition. It reuses the Research card frame and art well, places the offer name at the top-left, renders the coin or crystal art inside the well with the amount over the art in white outlined type, and reserves a fixed right slot for the shared green action button. Real-money prices and ready `Collect` actions use green; unavailable coin cooldowns use the shared disabled gray button without changing geometry. The button alone is interactive and owns the offer semantic target. States are available, unavailable/cooldown, notification, and hidden. Scope is Market currency offers. |
| Room inventory opener | `src/pages/shared/RoomInventoryButtonManager.js` | Shared icon-over-label side control for opening the seed, herb, or potion inventory surface. |
| RootRunSideAction | `WorkshopIconPanelAction`, `WorkshopFeatureButton`, `ROOT_RUN_SIDE_ACTION_GEOMETRY`, and `layoutSideControls` in `src/rendering/pixi/pages/workshop/WorkshopPixiPage.js`; DOM fallback in Workshop managers and `src/styles/base.css` | Approved Workshop icon-over-label side action. Its complete left/right stacks anchor `10px` from their owned source-stage edges. It keeps a `50x60px` source hit box while rendering one flat illustrated icon at `72%` inside its `50x50px` art frame and nudging the artwork another `2px` toward its owned screen edge. Centered Title Case `13.5px` Lilita One text uses the shared proportional near-black outline, a tight `-10px` icon-to-label offset, and a compact `62px` row pitch. Long labels may uniformly shrink to fit a `58px` visual width. Controls declare `side` plus numeric `weight`; visible controls pack from the shared top anchor without hidden gaps. The first row starts `18px` below Elara's Request, which is the approved slightly lowered placement. The shared brown/tan/gold/purple family covers a plain intact Alliance flag, podium, checklist and quill, satchel, star ledger, sealed envelope, telescope, and calendar wand. No icon has a feature-local backing plate, tint layer, mirroring, or independent optical scale. States are enabled, disabled, pressed, notified, timed, hidden, entering, exiting, and reduced-motion snap. Scope is the Workshop left/right HUD only. Approved evidence: `screenshots/root-run-side-actions-390x844.png`, `screenshots/root-run-side-actions-native-crop.png`, `screenshots/root-run-side-actions-desktop-fitted.png`, and `screenshots/root-run-side-actions-reference-review.html`. |
| Bottom room tab | `src/pages/bottomPanel/managers/BottomPanelViewManager.js`; `src/rendering/pixi/global/chrome/PixiBottomPanelView.js` | Fixed-width theme-matched top-cap tabs fill the source width and stretch their lower nine-slice region to the screen edge. Night keeps the original midnight assets; Day uses exact warm-brown recolors. The darker and lighter selected assets share `83 91 73 1` nine-slice geometry; active/inactive caps are `56px`/`44px` before the shared `26px` lower stretch. The selected tab is always `6px` wider, while each visible-tab count derives one fixed inactive width so the row remains edge-to-edge. Unlocked icons remain full-color and full-opacity, with non-selected icons rendered at `1.5x` their base artwork size. At seven visible tabs, every unlocked icon switches to `1.05x` inactive and `1x` selected compact scale. Selection applies the existing selected-icon treatment and lifts the icon `12px` over the retained `205ms` transition, while the active tab alone shows the white Title Case room name. Reduced motion snaps to the selected geometry. Locked tabs replace the room icon with a larger lock centered lower in the inactive frame. |
| Compact world chat | `src/pages/workshop/managers/WorkshopWorldChatManager.js`; `src/rendering/pixi/global/chrome/PixiWorldChatView.js` | Shared room chrome directly above the bottom tabs on every page after its existing level gate. Shows only the latest two one-line messages as one left-aligned block, vertically centered with a `2px` downward optical correction. System sender labels render as `System`. The complete title, preview, padding, and frame form one opener that uses the shared press-down haptic, validated release activation, held-release haptic, release animation, cancellation, and reduced-motion behavior before opening the existing full chat dialog without changing rooms. |
| WorldChatMessageRowPixi | `WorldChatMessageRowPixi` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved compact retained row for the full World Chat dialog. Player rows use a `22px` avatar, a two-line header/body rhythm, colored bracketed clan tag with the only text stroke in the row, dark-brown username/body copy, and a muted parchment-gold age at the far right. Chat rows use the paper width with an `8px` side inset instead of the generic dialog content column. Sparse message sets align their final row to the bottom of the viewport by default; overflowing histories retain the compact top inset and open pinned to the newest row. Player avatars and usernames open Player Info; the row never shows action-button chrome and never shows player level. System rows replace the avatar with the original parchment-colored borderless rounded background, use a darker-brown `System` title, and render a recognized announced-player name in a brown slightly lighter than the body copy. Only that announced-player name opens Player Info; the system title, announcement detail, background, and timestamp remain passive. Rows support player/system data, optional clan tag and color, wrapped body copy, timestamp, enabled Player Info action, and pooled reset/destroy states. Visual evidence: `screenshots/world-chat-approved-390x844.png`, `screenshots/world-chat-approved-native-crop.png`, `screenshots/world-chat-approved-desktop-fitted.png`, and `screenshots/world-chat-approved-reference-review.html`. |
| PixiInlineText | `src/rendering/pixi/primitives/PixiInlineText.js` | Shared retained inline flow for ordered text and icon runs inside compact canvas copy. Text and icons participate in the same measured wrapping layout; every icon reserves its actual rendered width and uses centered optical alignment within the line instead of overpainting placeholder glyphs. The API accepts plain text or ordered `{ kind: "text" }` / `{ kind: "icon" }` runs, wrap width, shared Pixi text style, icon size or dimensions, vertical optical offset, and text fallback for missing artwork. Rebinding reuses retained text/sprite children; empty runs clear the flow. Default, wrapped, multiple-run, missing-icon fallback, rebound, and pooled destroy states are supported. Intended scope is short compact copy such as chat and status messages, not interactive rich documents. First real-app consumer: World Chat prestige system announcements. Evidence: `src/dev/uiRecipes/world-chat-prestige.html` at authored `390x844`, with the focused renderer and primitive regression tests. |
| Potion Discovery Row | `PotionDiscoveryRowPixi` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved passive retained row for potion discoveries. It reuses the Root Run Settings nine-slice as a compact record backing, leads with potion art and a Title Case potion name, identifies the discoverer and UTC discovery date, presents the recipe as a two-column icon-and-quantity grid, and closes with mana, brew duration, and royalty metadata. The discoverer name alone opens Player Info. Undiscovered rows use muted mystery-potion art and hide recipe and economy details. Rows support discovered, undiscovered, interactive-discoverer, pooled reset, and destroy states; they do not add selected, disabled, loading, error, or motion variants. Initial scope is the Workshop Discoveries dialog. Visual evidence: `screenshots/discoveries-dialog-390x844.jpg`, `screenshots/discoveries-dialog-native-crop.jpg`, `screenshots/discoveries-dialog-desktop-fitted.jpg`, and `screenshots/discoveries-dialog-reference-review.html`. |
| AllianceDirectoryRow | `AllianceDirectoryRow` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved retained Trade Alliance directory disclosure row. Its collapsed summary keeps the chat-style colored bracketed alliance tag at the left, the alliance name beside it, and the all-time income plus shared coin icon right-aligned. Confirmed summary release toggles one inline expansion at a time. The expanded state owns a fixed `4.5`-member viewport, member count, and one action below the viewport: green `Join Alliance` or `Apply`, brown `Cancel Application`, or disabled gray `Closed`. Rows are keyed and retained across live backend snapshots so disclosure and nested scroll state are not rebuilt during refresh. States are collapsed, expanded, enabled, and disabled/closed. Scope is alliance-directory lists. Approved against the disposable `390x844` proposal on 2026-07-28. |
| AllianceMemberRow | `AllianceMemberRow` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved compact retained member row inside `AllianceDirectoryRow`. It shows player name, normalized alliance role, and `Lv N` in a fixed `28px` source row. The member viewport is exactly `4.5` rows high, scrolls independently with the shared retained scroll physics and scrollbar, and keeps keyed member widgets stable across snapshots. A player-name activation opens the existing Player Info surface when available; the row adds no action-button chrome. States are interactive player, passive player, pooled reset, and destroyed. Scope is expanded alliance-directory member lists. |
| WorldEventQuestRow | `WorldEventQuestRow` in `src/rendering/pixi/pages/workshop/WorkshopDialogPixi.js` | Approved retained World Event request row derived from the existing Research station card. One server request maps to one keyed image-backed row containing its Title Case quest name, points, narrative, progress/status, and up to four donation options. Each donation option keeps its item art, points-per-item, total, and shared compact `PixiCostButton`; available actions use the green shared skin and unavailable actions use its disabled gray skin. The row is pooled with explicit reset/destroy behavior and supports available, unavailable, completed, no-option, and overflowing-list states. Scope is the World Event `Quests` tab only. The dialog keeps event summary copy in a separate upper paper box, quest rows and their scrollbar in a separate lower paper box, and all three tabs inside the brown shell footer. Visual evidence: `screenshots/world-event-dialog-390x844.png`, `screenshots/world-event-dialog-native-crop.png`, `screenshots/world-event-dialog-desktop-fitted.png`, and `screenshots/world-event-dialog-reference-review.html`. |
| Popup text tab | `RetainedButton` with `variant: 'tab'`; `resolveDialogFooterTabLayout` and `setDialogPaperAboveFooterTabs` in `src/rendering/pixi/primitives/PixiDialogFrame.js`; `src/pages/shared/selectedTabState.js` and popup tab rules in `src/styles/base.css` for fallback markup | Brown text tabs with explicit selected semantics. Whole-dialog category rows stay inside the brown shell footer: `28px` high, `9px` side insets, `6px` paper gap, and `10px` bottom inset. Active, inactive, and disabled states reuse `brown-button-50.9.png`, `dark-brown-button-50.9.png`, and `gray-button-50.9.png` from the regular-button matrix with source slices `100 52 68 86` and compact output edges `13px 7px 9px 20px`; this leaves a live center band at the shorter height without duplicate tab PNGs. Equal footer-tab widths derive from count-aware gaps of `4px`/`6px`/`8px`/`10px` for five/four/three/two tabs. Content-local subsection controls such as Account Avatar/Frame reuse the same selected/unselected skins and semantics while remaining inside their paper instead of using footer placement. |
| Resource or item icon label | `src/pages/shared/resourceIconLabel.js`, `src/pages/shared/itemIconLabel.js` | Shared icon-before-text label that preserves surrounding normal/disabled text color and accessible text. |
| Seed pack icon | `src/assets/items/seeds/seedIconFrames.js`, `src/assets/items/seeds/seedIcons.js`, `src/rendering/pixi/primitives/PixiSeedPackIcon.js` | One shared composite icon across DOM and retained Pixi: the transparent seed-pack base plus the matching herb mark at `44%` scale, centered at `63%` of the pack height on the sack body, and rotated `6deg`. Callers may size the icon but must not replace its frame resolution, aspect-fit, or internal herb placement with feature-local geometry. |
| Info icon button | `src/pages/shared/infoButton.js` | Shared decorative info asset; caller owns accessible name, geometry, and disclosure behavior. |
| Lock/check status icon | `src/pages/shared/statusIcon.js` | Shared status artwork; accompanying control or text owns the accessible meaning. |
| Notification badge | `src/pages/shared/notificationBadge.js`; `PixiNotificationBadge` in `src/rendering/pixi/global/transient/PixiNotificationBadges.js` | Red/orange priority indicator with tutorial visibility policy; not a general decoration dot. Every DOM and retained-Pixi path uses the shared `notification-circle-red.png` / `notification-circle-orange.png` sprite at `12px`, matching Root Run's `36px` station badge at the project `/3` source scale. Regular buttons and text tabs keep the badge fully inside and tangent to the top-right edges so it stays clear of the label. Bottom room icon tabs retain the `4px` inset from both edges and keep their badges in the shared overlay above neighboring widgets. |
| Root Run Player HUD | `PixiTopPanelView` in `src/rendering/pixi/global/chrome/PixiTopPanelView.js` | Approved shared top-chrome compound at Root Run's exact qUIck `/3` geometry. It composes avatar/username at left, level rail above three equal currency capsules, mana generation rate below mana, and settings at right. The retained content root begins at authored `32/32` (`10.67/10.67` source); omit the reference export's separate notch offset because the interactive canvas already begins below the device safe area. The middle capsule is page-contextual and defaults to crystal. Its background uses the active theme's pixel-aligned top-panel nine-slice: the Night and Day assets are generated from their inactive room-tab caps with `30px` corners and the vertical flip baked into the PNG. Stretch the baked orientation edge-to-edge from source y `0…88`, just below the username and mana-rate labels, leaving a `16px` gap before room content begins at y `104`; do not flip it again in retained Pixi or CSS. The external safe-area strip and native Android status bar continue the active panel center color behind a cutout. FTUE may hide the full top group, background, avatar, level rail, or resource controls without reflow. Evidence: `screenshots/top-panel-compact-390x844.jpg`, `screenshots/top-panel-compact-native-crop.jpg`, `screenshots/top-panel-compact-desktop-fitted.jpg`, and `screenshots/top-panel-compact-reference-review.html`. |
| HUD Avatar Button | `RootRunHudAvatarButton` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `186x186px` qUIck-source avatar control using the Root Run nine-slice frame and head background. Player portraits use the generated square cuts in `assets/game/source/avatars`, with Elara as the fixed anatomical reference: face center and eye line align, head/hand scale stays consistent, and hats, hair, weapons, shields, or other decoration may clip. `npm run assets:avatars` rebuilds the cuts without modifying the full character art. The control supports character portrait replacement, equipped-frame tint, enabled press, FTUE visibility, and direct Account-dialog activation. The player username is composition-owned and centered immediately below this widget. |
| HUD Level Rail | `RootRunHudLevelRail` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `662x93px` qUIck-source level star and progress rail. The level star and complete visible rail form one press target that opens Level Rewards with shared pressed and tap feedback. It uses Root Run's exact fixed panel and rounded track plus the losslessly distilled authored yellow `53x42px` fill texture with `26 20 26 21` slices, rendered at the authored `51px` qUIck height. Only its single-pixel center stretches, while its alpha preserves both caps without a feature-local gradient. A request divider is temporarily omitted when it would collide with the live rounded cap; all other boundaries keep their thin separator. No visible progress fraction or remaining-count copy appears. It supports hidden-at-level-zero, partial fill, completed separators, rollover, and reduced-motion completion. |
| HUD Currency Capsule | `RootRunHudCurrencyCapsule` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `208x66px` qUIck-source image-backed resource capsule. It supports coin, crystal, ruby, emerald, and mana icons; compact formatted values; contextual resource replacement; and FTUE visibility. The compound HUD owns the optional rate label below mana. |
| HUD Square Icon Button | `RootRunHudSquareIconButton` in `src/rendering/pixi/global/chrome/RootRunTopHudWidgets.js` | Approved `122x122px` qUIck-source image-backed icon action. Its first consumer is the top-panel settings gear, with retained press routing, semantic activation, haptic feedback, and FTUE visibility. New icon meanings reuse this widget only when the same square silhouette and interaction contract are appropriate. |
| Tooltip | `.style-tooltip` in `src/styles/base.css` | Small active-theme explanatory surface with compact offset shadow; not a replacement for persistent labels. |
| Reward / spend flyout | `src/pages/shared/RewardFlyoutManager.js`; `PixiTransientEffectsLayer` in `src/rendering/pixi/global/transient/PixiTransientEffectsLayer.js`; Workshop notice variant in `src/rendering/pixi/pages/workshop/WorkshopPixiPage.js` | Shared pooled, bounded transient feedback with reduced-motion support. Visible text uses compact white Lilita One with a rounded near-black stroke over a 62%-opaque black `24px` squircle; inline item/resource art keeps its authored color inside the same backing. Successful foreground consumption uses the consumed resource icon and the Root Run seven-icon ballistic burst, anchored to the action that spent it; failed actions emit nothing. |
| Cauldron Carousel | `BrewingHudPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Unframed Brewing preview section. It centers one cauldron within the selected recipe's six-tile ingredient orbit, places the previous/next arrows just outside the lower-left and lower-right slots with a slight downward optical offset, keeps horizontal swipe, and anchors the cauldron title plus shared three-slot star rank inside the shared mana-blue title plaque connected to the screen's left edge. The empty preview does not repeat recipe or batch instructions; recipe identity appears after selection, while batch quantity remains in the existing `xN` control. Dynamic dots cover unlocked cauldrons plus one next locked slot; at the five-cauldron cap no extra locked dot is rendered. Selection changes use a compact directional cauldron settle without decorative trails or particles, regardless of whether navigation came from a chevron or swipe. The effect completes in `240ms`, never blocks input, clears on page deactivation, and snaps instantly under reduced motion. |
| Illustrated Action Button | `PixiButton` in `src/rendering/pixi/primitives/PixiButton.js`; regular skins in `assets/game/source/ui/regular-button`; optional retained icon compositions in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Shared image-backed action with blue, brown, dark-brown, gray, green, purple, red, and yellow asset colors. Every color exposes `50`, `30`, and `15` squircle-radius tiers with respective `171px`, `104px`, and `53px` minimum source heights including the bottom shadow and at least a `3x3` scalable center; use the smallest tier whose geometry fits the authored control height. Existing regular actions, cost-button compositions, and popup text tabs use the radius-50 textures. Disabled state swaps to gray artwork with no shader. A feature may lead the visible label with retained illustrated art while preserving the same bounds, semantic target, press motion, and accessible label. |
| Garden Seed Action Bar | `GardenSeedActionBar` in `src/rendering/pixi/pages/garden/GardenPixiPage.js` | Garden-local compound control that composes shared `PixiButton` actions with the shared room-panel and seed-pack icon contracts. `Plant All`, `Harvest All`, and `Seeds` use the shared `36px` room-control height and divide the row evenly according to which researched actions are visible. `Plant All` and `Harvest All` use the green positive-action skin; each stays actionable in its empty state so it can show shared feedback, and `Harvest All` shows the shared notification state only when harvests are available. `Seeds` uses the yellow highlighted-action skin and opens the retained seed picker. The compact centered selection panel is hidden when no seed is selected; otherwise it shows the selected seed-pack icon, name, and owned quantity. The bar has no loading, error, overflow, or feature-local motion variants. Scope is the Garden room action area. Evidence: `screenshots/garden-bulk-actions-390x844.jpg`, `screenshots/garden-bulk-actions-native-crop.jpg`, `screenshots/garden-bulk-actions-desktop-fitted.jpg`, `screenshots/garden-bulk-actions-reference-review.html`, plus focused retained-page and presenter tests. |
| Brewing Batch Detail Panel | `BrewingHudPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Brewing-only action section below the unframed preview, aligned to the shared `16px` room edge used by World Chat. A Research-row art well holds the enlarged predicted potion or remains an empty solid squircle, progress sits beside it, and one wide iconless primary button projects Brew, Cancel, Bottle, or Collect from the current manual/Auto process state. Recipes, compact `Auto`, and narrow `xN` form the right-packed configuration row at the shared `36px` room-control height. Locked Auto and batch-quantity controls are omitted without reserving slots. Auto Off is static yellow; Auto On is green and advances only its gear in short mechanical steps, stopping while hidden/off and snapping static for reduced motion. |
| Cauldron Ingredient Picker Slot | `BrewingIngredientPickerSlot` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Brewing-only interactive orbit slot using the ordinary room-panel skin. Every occupied slot represents exactly one herb and normally shows its item art and compact name. A selected-recipe slot whose unit ingredient is unavailable adds a compact top-right `0/1`; only the `0` uses the shared red state color, while staged/satisfied slots and active brews hide the ratio. Repeated recipe herbs occupy repeated ordered slots. Enabled slots open the shared `Choose Herb` inventory-choice dialog for the selected cauldron; choosing a herb replaces that one slot and closes the picker. Locked, full, and active-brew states keep the same geometry but reject input. There are no selected, loading, or feature-local motion states. Brewing always renders six slots and gameplay remains authoritative for accepting the chosen herb. Evidence: `screenshots/brewing-unit-slots-390x844.png`, `screenshots/brewing-unit-slot-native-crop.png`, `screenshots/brewing-unit-slots-desktop-fitted.png`, `screenshots/brewing-unit-slots-reference-review.html`, and focused retained-page/presenter tests. |
| Automation Toggle Row | `BrewingAutomationSettingsDialogPixi` in `src/rendering/pixi/pages/brewing/BrewingHudPixi.js` | Per-cauldron persisted auto-collect toggle inside the shared dialog shell. It is enabled only while autobrew is enabled and defaults off. |
| Transient Toast | page-local toast in `BrewingPixiPage` | Short non-modal feedback anchored over the Brewing HUD; visible for 1.6 seconds and never blocks input. Currently used by timer fast-forward to report `coming soon`. |

Brewing widget real-app evidence:
`screenshots/brewing-two-section-390x844.jpg`,
`screenshots/brewing-two-section-native-crop.jpg`,
`screenshots/brewing-two-section-desktop-fitted.jpg`, and
`screenshots/brewing-two-section-reference-review.html`.

The managed vertical scroll pane intentionally has one behavior with three sizing contexts. Do not create separate room, dialog, or list scroll physics unless a genuinely different interaction requires a new documented widget.

## Previewing Existing Widgets

- Prefer an existing `?devUi=<surfaceId>` route or checked-in UI recipe for full compositions. Add a new checked-in preview route only after its widget is approved.
- Proposal-only previews remain disposable. Approved implementation QA uses the real app at `390x844`, plus a native-pixel crop for the library entry.

## Box Construction

- The existing ordinary border-labeled box keeps compact padding, no added shadow, and its current themed frame contract. New default room landmarks and illustrated panels require separately approved image-backed widgets; do not mutate this legacy box into an all-purpose fantasy card.
- In selectable themes, standard `style-box` inner sections use `inner-section-panel-<theme>.9.png` with CSS slice order `91 73 90 83 fill`. Its rendered border widths are those same qUIck margins scaled from the `1080px` export width to the `390px` logical surface; never substitute arbitrary compact border widths. Keep the exact center-sample color behind the border image so fractional contain scaling cannot expose transparent hairlines at slice joins. The top panel is the explicit exception and uses the baked-flip `30px`-corner sibling of the inactive room-tab skin with source slices `L30 T1 R30 B30`. Do not reuse the inner-section skin for other outer `style-panel` surfaces, dialogs, buttons, tabs, tutorial surfaces, or feature-owned paper art.
- Box titles sit embedded transparently over the top border, without a background backplate.
- Box titles use the same shared proportional near-black text stroke as every other outlined label, keeping the border visible around the glyphs without creating a rectangular title surface.
- Secondary border labels, such as counts, current controls, actions, and tabs, use smaller normal-weight text centered on the border line.
- Default player-facing popup/dialog boxes use the shared Root Run Expedition dialog shell: fixed `304px` content width and `53px` minimum content height produce the standard `364px` brown outer nine-slice; `352px` Expedition paper nine-slice inset `6px` horizontally, `31px` from the shell top, and `21px` from the compact shell bottom; separate `222x44px` purple title plaque; `41px` round X close asset centered `23px` below the shell; and one compact bottom-right shadow. Named wider or fixed-height variants may override the base geometry. Plaque text uses the exported `64px` source type metric (`23.1px` at `390px`), the shared proportional text stroke (`5.33px` at that font size), and the source `22px` top offset. These image layers stay transparent behind their PNG corners; never add a full rectangular fallback fill beneath them.
- `RetainedGlobalDialog` consumers use the same shell with a stricter responsive cap: the complete brown frame is at most `324px`, leaving `5%` on both sides of the `360px` retained source screen. That yields a `304px` core and `264px` maximum content box after shared padding. Reflow rows, grids, and external tab spans to that width; do not scale the retained display tree.
- Backend boot and reconnect use the shared `PixiLoadingSplash` through `PixiOnlineGateView` until the backend reports online. Account-in-use, maintenance, account, and deploy blockers keep the shared player-dialog shell and remain non-dismissible when the flow must retain control.
- Every managed scroll panel uses `style-page-scroll` on the actual scrolling element. Full-page room roots additionally apply the shared `--style-page-scroll-padding-top` / `--style-page-scroll-padding-bottom` cuts so content tucks under chrome consistently. Retained dialog lists use the shared `12px` source top inset and keep image-backed rows inside the common horizontal content inset. Component-owned larger title clearance still wins. Dialogs, popups, and bounded lists use the same Root Run station scroll behavior and vertical scrollbar without introducing a second scroll type.
- Inner boards and image-backed row frames on standard retained dialog paper use an `8px` left and right inset. Treat the row frame and scrollbar gutter as one paper-contained layout group: extend the viewport `2px` beyond the row before the shared track gap, then place the overflow-only scrollbar inside the reserved right-side paper space.

## Numbered Rows

- Numbered game rows use a narrow left number cell, written as `1.`, `2.`, etc.
- Main item/content text sits in the middle.
- Price, status, timer, or action sits right-aligned in one fixed right slot.
- Never leave the middle content blank. Unlocked empty slot rows should invite the next action, such as `select` or `request item`; locked empty rows still name the thing, such as `empty stand` or `empty request`, with the right slot carrying requirement/state.
- Item market rows without numbers use the same compact middle/right rhythm as numbered rows; do not give stock/picker rows looser vertical padding.
- Do not write row type words such as `stand 1` when the surrounding box already gives the type.
- Compact garden plot and dialog rows follow this left-number, middle-content, right-action rhythm. Market page request/listing slots are the exception: both reuse the full retained Traders `ShopStallWidget` card contract, including the independent card frame, art well, quantity, resource-aware price, whole-card press feedback, and keyed pooling.

## Inline Row Actions

- Row actions are text controls on the line, not separate decorative cards.
- Purchase prices use the shared DOM `CostButtonManager` or retained `PixiCostButton`: the caller supplies the formatted cost, current availability, accessible action label, and purchase callback. The control owns the shared regular-button green/gray nine-slices, Lilita One outlined amount, canonical resource icon, icon-before-value geometry, disabled state, and click isolation; gameplay facades still own affordability checks and spending.
- Brewing `recipes`/primary/quantity/auto controls use the yellow Root Run button configuration through `style-button--yellow`. Workshop `Summon Seed` reuses the approved `92x52` stacked `PixiCostButton` content layout with an `11px` action label above a mana-icon-and-amount row and the shared purple regular-button nine-slice. The disabled state swaps to the shared gray regular-button nine-slice while preserving the text, press, input, and notification contracts. The Brewing locked-cauldron purchase uses the shared green regular-button nine-slice with `Unlock` above the canonical coin icon and amount; all other cauldron action buttons hide while that slot is selected. Workshop `bag`, `stats`, and the other left/right HUD entries use the approved `RootRunSideAction` contract and its `icon-side-*-root-run.png` family. Other purchase cost buttons stay green and popup tabs stay brown.
- Do not define mouse-hover states for row or button actions; press states must not tint row or button backgrounds.
- Underlines are allowed only for neutral selected options in mutually exclusive controls, such as tabs or one-of button panels; do not show them while held or pressed.
- Buy/place/claim prices stay right-aligned and use tabular numerals.
- Disabled or locked right-side actions use muted text and keep the same row height as enabled rows.

## Info Buttons

- Standalone info/help buttons use `assets/game/source/ui/prop_info.png` through the shared `setInfoButtonIcon` helper. Do not substitute `[i]`, `?`, or a font glyph.
- Callers keep ownership of the accessible `aria-label`, expanded state, tooltip/dialog action, and hit-target geometry. The shared helper owns only the visible icon and decorative-image semantics.
- Size the icon to the surrounding control: border-label info buttons use the border-label line height, while freestanding controls may use the full authored hit-target height.

## Lock Icons

- All visible lock iconography uses `assets/game/source/ui/prop_lock.png` through `createStatusIcon(..., STATUS_ICON_LOCK)`. Do not substitute CSS-drawn padlocks, Unicode locks, font glyphs, or alternate lock images.
- Keep meaningful player-facing `locked` text when it communicates state; this rule standardizes only the graphic.
- Callers continue to own the surrounding control's accessible label and geometry. Lock artwork remains decorative when the control or state label already exposes the locked state.

## Checkmark Icons

- All visible checkmark iconography uses `assets/game/source/ui/prop_checkmark.png` through `createStatusIcon(..., STATUS_ICON_CHECK)`. Do not substitute CSS-drawn checks, Unicode checks, native checkbox marks, font glyphs, or alternate checkmark images.
- Composite research icons that include a checkmark must source the same `prop_checkmark.png` artwork through `scripts/generate-research-icons.js`.
- Keep meaningful player-facing text such as `complete`, `claimed`, or `selected`; this rule standardizes only the graphic.
- Callers continue to own the surrounding control or state label's accessible name. The checkmark remains decorative when that name already communicates the state.

## Root Run Settings Controls

- `RootRunSettingsSliderPixi` in `src/rendering/pixi/primitives/PixiSettingsControls.js` is the shared settings slider. It uses a 14px progress rail so draggable allocation and settings controls read more clearly than passive 10px progress bars, plus the exact `assets/game/source/ui/root-run-settings/settings-knob.png` thumb at the documented 23px source size. `range` mode maps an integer `min`/`max`/`step` without visible milestones and accepts an optional `tutorialTargetValue` in the same units. `milestones` mode accepts ordered named options, renders dots only for interior stops, and snaps taps or horizontal drags to the nearest enabled option; the minimum and maximum remain selectable endpoints without redundant dots. Each newly reached milestone emits the shared light touch haptic after its change is accepted; unchanged stops, mouse movement, keyboard/semantic activation, disabled controls, and rejected changes remain silent. Milestone fill roles are empty for `none`, red for `low`, yellow for `medium`, and green for `high`; mana uses blue. Disabled controls keep their full-opacity knob and geometry while rejecting press, drag, and semantic activation. Pointer input converts the input router's global point into local rail coordinates, blocks page-swipe capture, deduplicates unchanged writes, and exposes semantic activation.
- `RootRunSettingsTogglePixi` in `src/rendering/pixi/primitives/PixiSettingsControls.js` is the shared compact `60x24px` boolean switch matching the Root Run Settings dialog. It uses the same exact 23px knob asset as the settings slider, the Root Run brown capsule rail, and the lime inset for `on`. It supports `off`, `on`, pressed, and disabled states with immediate state changes and semantic activation. Feature rows own their label and locked copy; when a feature is locked, omit the toggle and show `locked` in the existing right-side status slot.
- `RootRunDevicePreferencesPanel` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved light settings board inside the default white dialog paper. It uses the exact Root Run Settings nine-slice, `10px` board padding, and `50px` source-pitch rows without nested cards or scrolling. In the default Settings composition, Sound, Music, and Vibration share one board while Theme uses a separate one-row board with the standard `8px` section gap.
- `RootRunDevicePreferenceRow` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved icon-led settings row. It uses the exact Root Run sound, music, vibration, or theme icon, a `19px` Lilita One uppercase label, and the shared `RootRunSettingsTogglePixi` in a fixed right-side slot. It may swap between a pair of same-size state icons; Theme uses the matching moon for Night/off and sun for Day/on. Supported states are `on`, `off`, pressed, and disabled; current consumers are device-local `sfx`, `music`, and `haptics`, plus the account-persisted binary `theme`.
- `DeviceIdentityFooter` in `src/rendering/pixi/primitives/PixiDeviceSettingsWidgets.js` is the approved settings identity footer below the board. It centers the real client version, renders a compact connected Spacetime identity while retaining the full value for copy, and places a yellow Root Run `copy` action directly beside the id. It supports unavailable, ready, pending, copied, and retry states.
- The default `PixiSettingsDialog` configuration composition places the Google account heading, live auth status, and existing connect/disconnect action in its own light Root Run settings block after the separate device and Theme boards. Every section uses the standard `8px` gap. The account block reuses the exact Settings nine-slice, centered `PixiTextLabel` copy, and standard yellow `PixiButton`; unavailable and busy states use the shared disabled gray skin. Username and character customization remain in the separately titled Wizard surface. Evidence: `screenshots/settings-google-account-390x844.jpg`, `screenshots/settings-google-account-native-crop.jpg`, and `screenshots/settings-google-account-desktop-fitted.jpg`.

## Stall Allocation Lists

- Trader stall loaders use the exact Root Run Settings inner-panel backing as a nine-sliced background for every inventory row. Rows keep the Settings geometry: a 50px source pitch, 6px inter-row gap, and 8px left/right content padding.
- Tapping an inventory row selects one current item without moving stock. The selected row keeps resource/icon colors unchanged and shows the shared checkmark at the right edge with the same 8px inset.
- Load Stall extends the row's fixed right value slot with the current trader unit price. Owned items with a valid current trader price show the shared red notification badge, and each footer tab rolls up that state from its visible category rows. The badge remains independent of selection and does not replace the selected checkmark.
- Garden `Choose Seed` and Brewing `Choose Herb` reuse `RootRunInventoryChoiceDialogPixi` with the exact retained `RootRunInventoryChoiceList` from `ShopDialogPixi.js`: 28px item art, item name above `N Available`, identical row chrome and spacing, and the same optional selected checkmark. Both use one continuous paper containing only the catalogue. Choosing a Brewing herb assigns one item to the active cauldron slot and closes the picker. Both list viewports have a 4.5-row minimum height plus the shared top inset, so short lists stay top-aligned while preserving dialog height; longer lists scroll inside the same capped viewport. Evidence: `screenshots/brewing-herb-picker-single-paper-390x844.png`, `screenshots/brewing-herb-picker-single-paper-native-crop.png`, `screenshots/brewing-herb-picker-single-paper-desktop-fitted.png`, and `screenshots/brewing-herb-picker-single-paper-reference-review.html`.
- The allocation control is a Market-yellow progress rail with the exact Root Run Settings knob asset at 23px source size. Its range is the exact integer item count from `0` through all matching stock already loaded plus matching stock still in inventory, with one reachable stop per item and no duplicate count states. Do not show extra labels under the rail.
- The Load Stall action row contains two balanced controls: `clear` on the left and `mark xN` on the right. `mark xN` applies the selected target quantity in one gameplay update. `clear` returns loaded stock and stops any retained future marking. Successful mark and clear actions close the loader.
- A stand waiting for future production keeps its item target after loaded stock reaches zero and reads `waiting for <item>` instead of `empty stand`.

## Progress Rails

- All passive in-game progress and timer rails reuse the clean interiors of Root Run's `progress-bar-bg.9.png` and `progress-bar-fill.9.png` exports after stripping their one-pixel Android metadata borders: 10px source height, a 1px black capsule border, a dark inset rim, a 1px inner gap, and fully rounded alpha-masked fill caps. Progress assets do not define the retained scrollbar; that component uses Root Run's antialiased procedural Pixi geometry.
- Interactive sliders and the top-panel quest rail use 14px source height. Sliders preserve the shared capsule geometry and existing knob size.
- The default fill is purple `#8740df` with a lighter purple `#bd72f3` inset edge. Brewing overrides it with blue, Garden with green, and Market/Research with yellow. Trader-stall sale progress is the Market exception: it stays purple and does not inherit the gradient player style. The top-panel level rail uses Root Run's exact image-backed panel, rounded track, and authored yellow fill texture independent of the player's selected rail style, with thin request separators and no visible progress numbers. App-level connection/loading rails keep the selected gradient treatment. Alternate researched fill treatments keep the same rail geometry.
- Reveal progress by changing width or the live right edge. Never use `scaleX`, because it compresses the rounded caps.

## Expandable Boxes

- Collapsed boxes keep one full summary row visible inside the box.
- Use a top-right border label for count or progress, such as `2/5`.
- Use a bottom-center border label as the toggle: `expand` when collapsed, `collapse` when open.
- Expand hidden rows in normal flow inside the same box, without overlaying nearby panels.
- Workshop main progression uses one always-open `Elara's Request` box. Show only the active request; the shared top panel owns the approved HUD Level Rail with one segment per configured request and no progress-number caption. Request completion reuses Root Run's exact research-XP feedback contract in `PixiTopPanelView`: one 68px Root Run source star from the request surface to the level star, a distance-timed direct 96px source arc, progress held until arrival, an eight-spark arrival burst, and a 400ms destination impact. Retained Pixi maps those measurements through the shared `/3` source scale. Reduced motion skips directly to the completed state. Completing the final request automatically advances the player level; never relabel the request box or add a separate level-up action. Keep the whole progress row hidden at level 0 and reveal it after the automatic transition reaches level 1. Do not add XP copy, an expandable checklist, pinning, or task reordering.

## Popup Structure

- Default dialog titles sit centered in the separate Root Run purple title plaque above the brown shell; ordinary box titles remain embedded border titles.
- Dismissible dialogs use the shared round Root Run X asset centered below the shell at the authored gap; retain its accessible close label. Blocking dialogs use the same shell with the close action hidden.
- Back, previous, and next actions remain text controls. Never reuse a close class for navigation or replace navigation text with the X asset.
- Every whole-dialog category tab row sits fully inside the brown shell footer below the content paper. Use the shared `28px` row with `9px` side insets, `6px` of brown between paper and tabs, `10px` below the tabs, compact `11px` labels, and the shared brown/dark-brown/gray regular-button textures with tab-specific `13px` top and `9px` bottom output insets. Do not reuse the regular button's `17px + 12px` output geometry, which would collapse the center band. Use equal widths after count-aware gaps of `4px` for five, `6px` for four, `8px` for three, and `10px` for two. Apply the same footer boundary to continuous- and split-paper dialogs. Content-local subsection controls such as Account's internal tabs remain in their paper instead.
- True tab buttons use the shared selected-tab state so every tab has explicit selected and unselected views. Text tabs use the very-dark Root Run brown button skin when deselected and the lighter brown skin when selected; the five bottom room icon tabs alone use the active theme's full-width darker/lighter top-cap nine-slice row with an `8px` bottom safe gap. Do not assign tab semantics to previous/next pager buttons.
- Standard tabbed popups use `--style-tabbed-dialog-width` on the panel and `260px` content width on the dialog. Wider `286px` dialogs must use the wider panel token too; never mix a standard tab panel with a wider dialog.
- Tabbed dialogs keep a fixed bordered panel height; overflowing tab content scrolls inside that panel while the bottom tabs remain fixed.
- Every scrollable dialog pane puts `style-page-scroll` on the actual scrolling viewport. Wheel and drag input use the station resistance/inertia/spring model; the track/thumb appears at the right edge only when content overflows and the thumb compresses during elastic top/bottom overscroll. Never reserve bottom space or add a sibling scroll-progress rail.
- Popup rows should keep stable DOM nodes during snapshot updates so mobile taps do not miss after touchend.
- Book-like catalogues use compact column headers and stable selectable rows; selecting a row reveals a short detail/history block in the same dialog instead of stacking another catalogue modal.
