# Inbox Rewards

Local claim guard for server inbox rewards.

`InboxRewardsFacade` grants the reward payload attached to a player inbox mail and persists claimed mail keys so server collect retries cannot grant the same mail twice.
