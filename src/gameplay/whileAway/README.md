# While-Away Reports

Collects a transient summary around offline and foreground-resume timer catch-up. It does not own gameplay rules or persistence; it observes existing gameplay callbacks and queues produced item and NPC market sale rows for page UI to consume.
