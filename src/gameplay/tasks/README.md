# Tasks

Tasks turn item drops into player level progress.

Each level has five tasks from SpacetimeDB `game_config.tasks`. A task consumes owned items until its required quantity is full, then it can be completed. Completing all five tasks on the current level advances the player level.
