# Brewing Page

Brewing is the left edge room page, immediately left of Garden.

It renders herb drag/drop, cauldron contents, brew, bottle, and collect actions, `recipes` and `potions` popup buttons, an `auto brewing` popup for selecting the automatic recipe and enabled state, a marked recipe guide inside the cauldron, navigation, and the bottom page name.
It does not own potion recipes, brewing costs, brew timers, inventory changes, effects, or progression rules; those stay in `src/gameplay/brewing/`.
