# Alliance Info Dialog

Shows a compact read-only popup for a public alliance when an alliance row or tag is pressed. It reads public alliance/member data from the trade alliance backend and can offer `join` or `apply` when the viewer is not already in an alliance.
