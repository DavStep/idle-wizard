# UI Click Sound

UI click sound owns the small feedback sound for actionable page controls.

It is intentionally not gameplay audio. Page input code tells the facade when a usable UI press happened, and the manager plays a short procedural Web Audio click. No borrowed binary sound asset is required.
