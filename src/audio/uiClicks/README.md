# UI Click Sound

UI click sound owns the small feedback sound for actionable page controls.

It is intentionally not gameplay audio. Page input code tells the facade when a usable UI press happened, and the manager plays the same small pop sample plus short tone used by Idle Witch Craft's `button-tap` cue.
