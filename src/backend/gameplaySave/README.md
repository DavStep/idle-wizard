# Gameplay Save Backend

Stores the full gameplay save JSON in SpacetimeDB under the connected identity.

The gameplay layer still creates and applies the save shape. This backend feature only moves that save to and from the server table and reducer.
