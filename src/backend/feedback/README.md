# Feedback Backend

Stores free-form player feedback in SpacetimeDB through the `submit_feedback` reducer.

The game only sends feedback. Future dashboard work can add a guarded review surface without exposing generated database APIs to page UI.
