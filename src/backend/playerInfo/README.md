# Player Info Backend

Subscribes to the bounded public `player_info_summary` view so UI can show a compact profile for players already visible in social surfaces. It exposes display stats only: username, alliance tag, total produced gold, level, and prestige count.
