# Player

The player feature owns in-memory player identity and visual preferences shown in room UI.

SpacetimeDB profile sync stores `username`, visual theme, font, fixed resource color mode, and username prompt state. Client-only visual choices such as progress bar style live here too, but are not gameplay rules. Icons are fixed on. Player does not own resources, economy, or gameplay rules.
