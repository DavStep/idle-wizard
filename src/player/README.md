# Player

The player feature owns in-memory player identity and visual preferences shown in room UI.

SpacetimeDB profile sync stores `username`, visual theme, font, color mode, and username prompt state. Player does not own resources, economy, or gameplay rules.
