# Player

The player feature owns in-memory player identity and visual preferences shown in room UI.

SpacetimeDB profile sync stores `username`, visual theme, font, color mode, and username prompt state. Client-only visual choices such as icons and progress bar style live here too, but are not gameplay rules. Player does not own resources, economy, or gameplay rules.
